---
phase: 02-validated-run-and-cohort-admission
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - projects/spatial-pharma-dl/src/validation.py
  - projects/spatial-pharma-dl/src/data.py
  - projects/spatial-pharma-dl/src/labels.py
  - projects/spatial-pharma-dl/src/patches.py
  - projects/spatial-pharma-dl/scripts/run_pipeline.py
  - projects/spatial-pharma-dl/tests/test_cohort_admission.py
autonomous: true
requirements:
  - VAL-04
must_haves:
  truths:
    - "D-02: Every admitted run contains the Plan 01 canonical ResolvedConfig and a deterministic JSON-safe CohortManifest whose configured, included, skipped, and failed collections preserve configuration order."
    - "D-03: Strict admission aggregates all known missing or failed configured slides before downstream scientific work; remote-source admission remains provisional until source outcomes are known, strict source failure raises CohortAdmissionError with a deterministic failed manifest and publishes nothing, and explicit partial mode re-admits once before publishing the final usable cohort."
    - "D-05: The existing run_pipeline.py entry point, environment flags, stage order, output names, helper signatures, and public imports remain stable while one admitted slide sequence becomes the source of truth downstream."
  artifacts:
    - path: projects/spatial-pharma-dl/src/validation.py
      provides: Immutable slide, manifest, and admitted-run records plus pure strict/partial admission
      exports: [SlideAdmission, CohortManifest, AdmittedRun, admit_run]
    - path: projects/spatial-pharma-dl/scripts/run_pipeline.py
      provides: Resolve-overrides-admit startup flow and propagation of one admitted cohort
    - path: projects/spatial-pharma-dl/tests/test_cohort_admission.py
      provides: Offline strict, partial, manifest, runner-order, and fail-before-side-effect evidence
  key_links:
    - from: projects/spatial-pharma-dl/scripts/run_pipeline.py
      to: projects/spatial-pharma-dl/src/validation.py
      via: all environment overrides are applied before resolve_config and admit_run
    - from: projects/spatial-pharma-dl/src/validation.py
      to: projects/spatial-pharma-dl/src/data.py
      via: admitted slide IDs are the only sequence passed to cohort stages
    - from: projects/spatial-pharma-dl/src/data.py
      to: projects/spatial-pharma-dl/src/labels.py
      via: local catch-and-continue behavior is removed so partial policy cannot diverge by stage
    - from: projects/spatial-pharma-dl/tests/test_cohort_admission.py
      to: projects/spatial-pharma-dl/scripts/run_pipeline.py
      via: monkeypatched lightweight stage seams prove admission precedes side effects and propagates stable membership
---

<objective>
As a maintainer, I want one fail-closed cohort admission result used by every pipeline stage so that configured slides cannot disappear silently and an explicit partial run remains fully reconstructable.

Purpose: Deliver the VAL-04 vertical slice on top of the canonical configuration contract.
Output: Immutable manifest/admitted-run records, strict and partial policy, runner preflight wiring, removal of independent skip policies, and focused offline evidence.
</objective>

<execution_context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-validated-run-and-cohort-admission/02-CONTEXT.md
@.planning/phases/02-validated-run-and-cohort-admission/02-RESEARCH.md
@.planning/phases/02-validated-run-and-cohort-admission/02-VALIDATION.md
@.planning/phases/02-validated-run-and-cohort-admission/02-PATTERNS.md
@.planning/phases/02-validated-run-and-cohort-admission/02-01-PLAN.md
@.planning/phases/01-offline-verification-harness/01-VERIFICATION.md
</execution_context>

<threat_model>
  <threat id="T-01" severity="high" blocking="true">Invalid configuration reaches side-effectful pipeline work. The runner must re-resolve all environment overrides before admission or heavy imports.</threat>
  <threat id="T-02" severity="high" blocking="true">Canonical output contains arbitrary objects or unstable facts. Manifest serialization must contain only deterministic primitives and exclude timestamps, host paths, traceback text, sets, and exception reprs.</threat>
  <threat id="T-03" severity="high" blocking="true">Missing configured slides are silently dropped. Complete known-availability preflight must aggregate every unavailable member before any slide is processed.</threat>
  <threat id="T-04" severity="high" blocking="true">Partial mode obscures exclusions or failures. Require explicit allow_partial=true and preserve complete configured, included, skipped, and failed collections with stable reason codes.</threat>
  <threat id="T-05" severity="high" blocking="true">An empty or LOSO-unusable admitted cohort reaches scientific code. Admission must reject zero included slides and an admitted oncology benchmark with fewer than two slides before stage work.</threat>
  <security_gate>Block completion while any T-01 through T-05 high-severity threat can reach a stage without the specified preflight, deterministic manifest evidence, or fail-closed error.</security_gate>
</threat_model>

<tasks>

<task type="auto" id="2-02-01">
  <name>Implement immutable cohort manifests and pure strict or partial admission</name>
  <files>projects/spatial-pharma-dl/src/validation.py, projects/spatial-pharma-dl/tests/test_cohort_admission.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/validation.py</file>
    <file>projects/spatial-pharma-dl/src/data.py</file>
    <file>projects/spatial-pharma-dl/configs/default.yaml</file>
    <file>projects/spatial-pharma-dl/tests/conftest.py</file>
    <file>projects/spatial-pharma-dl/tests/test_validation.py</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-RESEARCH.md</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-PATTERNS.md</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-VALIDATION.md</file>
  </read_first>
  <action>Extend `src.validation` with frozen slotted `SlideAdmission(slide_id, cohort, status, reason_code=None, reason=None)`, `CohortManifest(schema_version, allow_partial, configured, included, skipped, failed)`, and `AdmittedRun(config, manifest)` records. Set schema version to exactly `cohort-manifest-v1`; add deterministic `canonical_json` and fresh `to_dict()` behavior to CohortManifest and an ordered `slide_ids` tuple property to AdmittedRun. Implement `admit_run(cfg, *, available_slide_ids=None, failures=None)` as a pure injectable boundary: resolve cfg, enumerate configured members in oncology/external/benchmark order, preserve that order in every collection, treat availability None as not locally knowable, aggregate supplied missing IDs and failure reasons, and use stable codes including `missing_processed_slide`, `missing_patch_cache`, and `source_load_failed` with separate concise guidance. Strict mode raises one CohortAdmissionError carrying the complete manifest and all unavailable/failed members; partial mode requires the explicit true flag, returns only included members, and may represent a source failure independently in failed and skipped. Reject a partial result with zero included slides or fewer than two admitted oncology slides when the public oncology LOSO benchmark remains requested. Do not add timestamps, paths, tracebacks, exception reprs, checksums, atomic publication, fingerprints, identity repair, or class-support rules. Create `test_cohort_admission.py` under the `offline` marker using synthetic mappings and injected collections to prove order, strict aggregation, explicit opt-in, complete reason-coded partial manifests, failure/skipped distinction, unusable partial rejection, deterministic JSON, and mutation-isolated `to_dict()` results.</action>
  <acceptance_criteria>
    <criterion>The named frozen slotted records and admit_run exist, AdmittedRun.slide_ids is an ordered tuple, and CohortManifest.schema_version serializes as `cohort-manifest-v1`.</criterion>
    <criterion>Strict admission with multiple unavailable or failed slides raises once, carries a manifest naming all of them, and returns no AdmittedRun.</criterion>
    <criterion>Partial admission is possible only with cohort_policy.allow_partial=true and records every configured member plus ordered included, skipped, and failed records with stable reason codes and separate guidance.</criterion>
    <criterion>Availability-set iteration cannot reorder output; configuration order controls all manifest sequences and admitted slide IDs.</criterion>
    <criterion>Zero included slides and fewer than two admitted oncology slides fail before downstream work.</criterion>
    <criterion>Canonical manifest JSON contains no timestamp, host path, traceback, set, arbitrary object, or non-finite value, and the focused command exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_cohort_admission.py</automated>
  </verify>
  <done>Pure admission deterministically decides one usable cohort or raises one complete fail-closed error without filesystem or network access.</done>
</task>

<task type="auto" id="2-02-02">
  <name>Wire startup admission and propagate one canonical cohort</name>
  <files>projects/spatial-pharma-dl/src/data.py, projects/spatial-pharma-dl/scripts/run_pipeline.py, projects/spatial-pharma-dl/tests/test_cohort_admission.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/validation.py</file>
    <file>projects/spatial-pharma-dl/src/data.py</file>
    <file>projects/spatial-pharma-dl/scripts/run_pipeline.py</file>
    <file>projects/spatial-pharma-dl/src/patches.py</file>
    <file>projects/spatial-pharma-dl/src/benchmark.py</file>
    <file>projects/spatial-pharma-dl/tests/test_verification_contract.py</file>
    <file>projects/spatial-pharma-dl/tests/test_cohort_admission.py</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-PATTERNS.md</file>
  </read_first>
  <action>Add a non-creating local availability helper in `data.py` that checks expected processed-slide paths using `safe_filename` without calling `pharma_processed_dir()` or mkdir; keep all existing public function signatures. Refactor `run_pipeline.py` so module scope retains only bootstrap, environment/path setup, and light data/validation imports; move matplotlib, pandas, benchmark, evaluation, labels, patches, model/foundation, and other heavy stage imports into `main()` after validation/admission. Inside `main()`, load config, apply both PHARMA_FOUNDATION and PHARMA_QUICK overrides, call resolve_config again, determine train-only/cache-backed known availability before directory creation, and call admit_run. Treat `available_slide_ids=None` for a normal remote curation run as provisional admission only: do not create the pharma output directory and do not publish `cohort_manifest.json` from that provisional result. In strict mode, attempt source preprocessing through the existing seam; if a configured source fails, convert that outcome into `CohortAdmissionError` carrying a deterministic CohortManifest whose failed record names the slide with reason_code `source_load_failed` and concise guidance, publish no manifest or success outputs, print no `Pipeline complete.`, and stop before summary, labels, patches, stain fitting, benchmark, model, encoder, or report work. When and only when allow_partial is true, attempt every configured source, collect the complete ordered successful-ID set and all concise `source_load_failed` reasons, call admit_run exactly once more with those complete outcomes, and treat only that returned result as final admission. Set `all_slides = list(final_admitted.slide_ids)`, derive oncology by filtering final manifest records with cohort equal to `oncology`, and pass those exact lists through every later stage without re-expanding cfg cohorts. Only after final admission succeeds, write its deterministic canonical manifest text to additive `cohort_manifest.json` in the existing pharma output directory; include no claim of atomicity, checksum, fingerprint, or completion-marker safety because Phase 4 owns durable publication. Preserve PHARMA_TRAIN_ONLY, PHARMA_FORCE_PATCHES, stage print order, the `Pipeline complete.` terminal message on success only, existing report/summary names, and existing CLI entry point. Tests must import the runner with heavy modules absent, monkeypatch resolution/admission and every downstream stage, prove invalid admission calls no side-effect seam, prove overrides are re-resolved before admission, prove provisional remote admission never writes, prove strict source failure raises CohortAdmissionError with the exact failed manifest and reaches no downstream/writer seam, prove partial source outcomes are fully collected and re-admitted exactly once before publication, and prove every successful downstream stage receives the same final admitted sequence and derived oncology subset.</action>
  <acceptance_criteria>
    <criterion>Importing run_pipeline does not import torch, torchvision, sklearn, skimage, timm, transformers, or a foundation encoder before main admission succeeds.</criterion>
    <criterion>PHARMA_FOUNDATION and PHARMA_QUICK mutate the fresh config before the final resolve_config and admit_run calls; quick epochs=2 and patience=1 remain the documented values.</criterion>
    <criterion>Known train-only availability is scanned completely without creating directories, and strict failure invokes no stage, directory, loader, model, encoder, or writer seam.</criterion>
    <criterion>Remote curation with available_slide_ids=None is explicitly provisional: it creates no output directory and writes no `cohort_manifest.json` before source outcomes are final.</criterion>
    <criterion>A strict remote source failure raises CohortAdmissionError carrying deterministic configured, included, skipped, and failed collections; the failed slide has reason_code `source_load_failed`, no false-success manifest or terminal message is written, and no downstream scientific stage runs.</criterion>
    <criterion>In explicit partial curation mode, every configured source outcome is collected in configuration order, all failures use `source_load_failed`, admit_run is called exactly once with the complete outcomes after provisional admission, and no later stage receives a failed member.</criterion>
    <criterion>Every successful downstream stage receives the ordered final admitted slide sequence, and stain/benchmark oncology is derived from admitted manifest ownership rather than cfg["cohorts"]["oncology"].</criterion>
    <criterion>After and only after successful final admission, `cohort_manifest.json` contains exactly the final deterministic CohortManifest data; no manifest is written for provisional or rejected admission and no atomic/fingerprint guarantee is asserted.</criterion>
    <criterion>The runner retains its path, main entry point, environment flags, output names, and successful stage/terminal messages.</criterion>
    <criterion>The focused admission and existing verification contract tests exit 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_cohort_admission.py projects/spatial-pharma-dl/tests/test_verification_contract.py</automated>
  </verify>
  <done>The CLI crosses resolve-overrides-admit before expensive imports or writes and uses one admitted membership throughout the run.</done>
</task>

<task type="auto" id="2-02-03">
  <name>Remove independent downstream missing-slide policies</name>
  <files>projects/spatial-pharma-dl/src/data.py, projects/spatial-pharma-dl/src/labels.py, projects/spatial-pharma-dl/src/patches.py, projects/spatial-pharma-dl/tests/test_cohort_admission.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/data.py</file>
    <file>projects/spatial-pharma-dl/src/labels.py</file>
    <file>projects/spatial-pharma-dl/src/patches.py</file>
    <file>projects/spatial-pharma-dl/src/validation.py</file>
    <file>projects/spatial-pharma-dl/scripts/run_pipeline.py</file>
    <file>projects/spatial-pharma-dl/tests/test_cohort_admission.py</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-CONTEXT.md</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-RESEARCH.md</file>
  </read_first>
  <action>Replace every `sample_ids = sample_ids or cohort_slide_ids(...)` and touched `cfg = cfg or load_config()` with explicit None branches so supplied empty values are not reinterpreted. Remove FileNotFoundError catch-and-continue behavior from `cohort_summary`, `build_labels_cohort`, `fit_reference_stain`, and `build_patch_cohort`; these helpers must process exactly their admitted input or propagate an actionable failure, never choose a new partial cohort locally. Keep the existing parameter lists, return types for valid inputs, label/patch filenames, domain-annotation filename, and print messages for successful work. Extend admission tests with monkeypatched load/build functions to prove a missing admitted member raises rather than prints-and-skips, partial filtering happens once before these helpers, and data/label/patch stages cannot independently produce divergent membership. Do not add empty-boundary guards owned by Plan 03 beyond the minimum admission checks already implemented, and do not change alignment, class support, scaling, imputation, stain science, or serialization policy.</action>
  <acceptance_criteria>
    <criterion>No touched cohort helper uses truthiness to replace an explicit empty config or sample sequence.</criterion>
    <criterion>`cohort_summary`, `build_labels_cohort`, `fit_reference_stain`, and `build_patch_cohort` contain no FileNotFoundError catch-and-continue path for configured/admitted members.</criterion>
    <criterion>A missing member passed directly to each helper raises before later members are processed; no success-completion message is emitted.</criterion>
    <criterion>An explicitly filtered partial sequence is used unchanged by all four helpers and preserves valid output filenames and return types.</criterion>
    <criterion>The focused admission tests and Ruff command exit 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_cohort_admission.py &amp;&amp; python -m ruff check projects/spatial-pharma-dl/src/validation.py projects/spatial-pharma-dl/src/data.py projects/spatial-pharma-dl/src/labels.py projects/spatial-pharma-dl/src/patches.py projects/spatial-pharma-dl/scripts/run_pipeline.py projects/spatial-pharma-dl/tests/test_cohort_admission.py</automated>
  </verify>
  <done>Admission is the only cohort-filtering policy, so every downstream stage sees the same explicit membership or fails visibly.</done>
</task>

</tasks>

<verification>
1. Run `python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_validation.py projects/spatial-pharma-dl/tests/test_cohort_admission.py`.
2. Run the existing `projects/spatial-pharma-dl/tests/test_verification_contract.py` offline module to protect the CLI verification contract.
3. Run Ruff over every modified source, script, and test file.
4. Confirm no durable/atomic manifest publication, cache fingerprint, unsafe-format migration, identity alignment, class policy, or leakage fix was introduced.
</verification>

<success_criteria>
- Strict admission reports every known unavailable configured slide before processing any member.
- Explicit partial admission records every configured, included, skipped, and failed member with stable reasons and rejects an unusable result.
- The runner resolves all overrides and admits before expensive imports, directory creation, data/model access, or writes.
- One ordered admitted slide sequence, including an admitted oncology subset, is used across all stages.
- Existing CLI, environment flags, output names, helper signatures, notebook order, and public imports remain compatible.
- No high-severity T-01 through T-05 threat remains open.
</success_criteria>
