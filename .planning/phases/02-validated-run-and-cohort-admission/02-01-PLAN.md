---
phase: 02-validated-run-and-cohort-admission
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/spatial-pharma-dl/configs/default.yaml
  - projects/spatial-pharma-dl/src/validation.py
  - projects/spatial-pharma-dl/src/data.py
  - projects/spatial-pharma-dl/tests/test_validation.py
autonomous: true
requirements:
  - VAL-01
must_haves:
  truths:
    - "D-01: One malformed configuration raises one ConfigValidationError containing every issue in deterministic schema order, with dotted paths, received values, expected constraints, and corrective guidance before a side-effect seam is called."
    - "D-02: Successful resolution exposes immutable canonical JSON that is byte-identical for semantically identical mappings, preserves cohort-list order, rejects non-JSON values, and returns a fresh mutable plain dictionary for existing callers."
    - "D-05: load_config(path=None) keeps its documented path and dict return contract, existing keys remain valid, and the only additive default key is cohort_policy.allow_partial=false."
  artifacts:
    - path: projects/spatial-pharma-dl/src/validation.py
      provides: Dependency-free exception hierarchy, immutable validation records, canonical JSON conversion, and aggregate configuration resolution
      exports: [PharmaValidationError, ConfigValidationError, ValidationIssue, ResolvedConfig, resolve_config]
    - path: projects/spatial-pharma-dl/configs/default.yaml
      provides: Explicit fail-closed cohort admission policy default
    - path: projects/spatial-pharma-dl/src/data.py
      provides: Existing load_config facade routed through strict resolution
    - path: projects/spatial-pharma-dl/tests/test_validation.py
      provides: Offline aggregate, canonicalization, compatibility, and fail-before-side-effect evidence
  key_links:
    - from: projects/spatial-pharma-dl/src/data.py
      to: projects/spatial-pharma-dl/src/validation.py
      via: load_config delegates parsed YAML to resolve_config and returns ResolvedConfig.to_dict()
    - from: projects/spatial-pharma-dl/configs/default.yaml
      to: projects/spatial-pharma-dl/src/validation.py
      via: cohort_policy.allow_partial is recognized as a boolean with a false default
    - from: projects/spatial-pharma-dl/tests/test_validation.py
      to: projects/spatial-pharma-dl/src/validation.py
      via: malformed and reordered synthetic mappings exercise the public resolution contract
---

<objective>
As a maintainer, I want every experiment configuration resolved and validated as one deterministic startup contract so that malformed values cannot reach pipeline side effects and admitted configuration can support later provenance.

Purpose: Deliver the VAL-01 vertical slice while preserving the existing mutable-dictionary facade used by notebooks and the runner.
Output: A dependency-free validation module, one explicit fail-closed policy default, load_config delegation, and focused offline evidence.
</objective>

<execution_context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-validated-run-and-cohort-admission/02-CONTEXT.md
@.planning/phases/02-validated-run-and-cohort-admission/02-RESEARCH.md
@.planning/phases/02-validated-run-and-cohort-admission/02-VALIDATION.md
@.planning/phases/02-validated-run-and-cohort-admission/02-PATTERNS.md
@.planning/phases/01-offline-verification-harness/01-VERIFICATION.md
</execution_context>

<threat_model>
  <threat id="T-01" severity="high" blocking="true">Invalid configuration reaches side-effectful pipeline work. Aggregate schema issues through pure resolve_config before directory, data, model, or writer seams.</threat>
  <threat id="T-02" severity="high" blocking="true">Canonical output contains arbitrary Python objects, non-finite values, or unstable ordering. Admit only JSON-safe primitives, sort mapping keys without sorting lists, and round-trip through strict canonical JSON.</threat>
  <threat id="T-03" severity="high" blocking="true">Missing configured slides are silently dropped. Preserve fail-closed defaults and leave all filtering to the single admission policy implemented in Plan 02.</threat>
  <threat id="T-04" severity="high" blocking="true">Partial mode hides exclusions or failures. Validate only an explicit boolean policy flag here; Plan 02 must produce complete reason-coded manifest evidence before partial execution is allowed.</threat>
  <threat id="T-05" severity="high" blocking="true">Empty scientific work reaches expensive code. Provide the shared StageValidationError and require_non_empty vocabulary without wiring later stage boundaries until Plan 03.</threat>
  <security_gate>Block completion while any T-01 through T-05 high-severity control assigned to this plan is absent, bypassable, or contradicted by a focused test.</security_gate>
</threat_model>

<tasks>

<task type="auto" id="2-01-01">
  <name>Create the validation errors, canonical records, and aggregate schema resolver</name>
  <files>projects/spatial-pharma-dl/src/validation.py, projects/spatial-pharma-dl/tests/test_validation.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/configs/default.yaml</file>
    <file>projects/spatial-pharma-dl/src/data.py</file>
    <file>projects/spatial-pharma-dl/src/foundation.py</file>
    <file>projects/spatial-pharma-dl/src/foundation_eval.py</file>
    <file>projects/spatial-pharma-dl/tests/conftest.py</file>
    <file>projects/spatial-pharma-dl/tests/test_fixture_contracts.py</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-RESEARCH.md</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-PATTERNS.md</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-VALIDATION.md</file>
  </read_first>
  <action>Create import-light `src.validation` using only the standard library. Define `PharmaValidationError(ValueError)`, `ConfigValidationError`, `CohortAdmissionError`, and `StageValidationError`; frozen slotted `ValidationIssue(path, received, expected, guidance)` and `ResolvedConfig(canonical_json)` records; `ResolvedConfig.to_dict()` returning a fresh object through `json.loads`; a strict JSON canonicalizer using `sort_keys=True`, `separators=(",", ":")`, `ensure_ascii=False`, and `allow_nan=False`; and `resolve_config(raw)` that first merges only documented optional defaults, then accumulates all schema issues in traversal order and raises once. Validate the complete table in 02-RESEARCH.md: required known mappings and unknown keys, dynamic names under marker_genes/gene_modules, non-empty unique cohort strings with no overlap, combined cohort non-empty and oncology length at least two, bool-versus-number strictness, finite/range/enumeration rules, n_pcs_neighbors less than or equal to n_pcs, nonzero combined task weights, label/module/gene rules, model/device/foundation registries copied as lightweight constants, and evaluation metric lists. Reject coercion, non-string mapping keys, sets, arbitrary objects, and non-finite values; preserve all list order. Start `test_validation.py` with module-level `pytestmark = pytest.mark.offline` and tests proving one malformed mapping reports multiple ordered dotted paths in one exception, bool is rejected as numeric, unknown known-map keys are rejected while dynamic panel/module keys are accepted, cross-field violations aggregate, reordered mapping keys produce identical canonical JSON, reordered cohort lists do not, unsupported Python values fail closed, and every `to_dict()` call is a fresh mutable nested dictionary. Include a lightweight-import assertion that importing `src.validation` does not load torch, scanpy, squidpy, torchvision, timm, or transformers.</action>
  <acceptance_criteria>
    <criterion>`src.validation` contains the named exception classes, frozen slotted ValidationIssue and ResolvedConfig records, resolve_config, and no scientific or model-library import.</criterion>
    <criterion>A single malformed mapping raises one ConfigValidationError whose `issues` attribute contains every expected dotted path in deterministic schema traversal order and whose text includes each received value, expected constraint, and guidance.</criterion>
    <criterion>Numeric validators reject bool, strings are never coerced, non-finite numbers and unsupported objects fail, and unknown keys are rejected except dynamic marker_genes and gene_modules names.</criterion>
    <criterion>Semantically identical mappings with different key insertion order produce byte-identical canonical JSON, while configured cohort list order is preserved and observable.</criterion>
    <criterion>Two calls to ResolvedConfig.to_dict() return equal but independently mutable plain dict/list trees.</criterion>
    <criterion>The focused validation command exits 0 under the offline guard.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_validation.py</automated>
  </verify>
  <done>The pure resolver reports all configuration defects together and returns deterministic JSON-safe state without importing or invoking expensive pipeline code.</done>
</task>

<task type="auto" id="2-01-02">
  <name>Route the existing configuration facade through strict resolution</name>
  <files>projects/spatial-pharma-dl/configs/default.yaml, projects/spatial-pharma-dl/src/data.py, projects/spatial-pharma-dl/tests/test_validation.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/configs/default.yaml</file>
    <file>projects/spatial-pharma-dl/src/data.py</file>
    <file>projects/spatial-pharma-dl/src/validation.py</file>
    <file>projects/spatial-pharma-dl/scripts/run_pipeline.py</file>
    <file>projects/spatial-pharma-dl/tests/test_validation.py</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-CONTEXT.md</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-PATTERNS.md</file>
  </read_first>
  <action>Add exactly `cohort_policy: {allow_partial: false}` in `configs/default.yaml` without renaming or relocating any existing key. Update `data.load_config(path: Path | None = None) -> dict[str, Any]` to use `yaml.safe_load`, pass its raw result to `resolve_config`, and return `ResolvedConfig.to_dict()`; retain `CONFIG_PATH`, the default path, call signature, and plain-dict behavior. Replace `cfg = cfg or load_config()` with `if cfg is None: cfg = load_config()` in `cohort_slide_ids` and any configuration-only helper touched in this task so an explicitly supplied `{}` is validated or fails rather than reloading defaults. Extend `test_validation.py` to write valid and invalid YAML beneath `tmp_path`, assert default.yaml resolves with `allow_partial is False`, assert load_config returns fresh mutable dictionaries, assert empty YAML and malformed YAML fail through ConfigValidationError, and monkeypatch a sentinel directory/data/model/writer seam that must remain uncalled when startup resolution fails. Do not change CLI names, notebook files, output names, `src.__init__`, cohort filtering, or any Phase 3+ scientific policy.</action>
  <acceptance_criteria>
    <criterion>`default.yaml` retains every existing top-level section and adds only `cohort_policy.allow_partial: false` for Phase 2.</criterion>
    <criterion>`load_config()` and `load_config(custom_path)` retain the same signatures and return fresh ordinary dictionaries containing all validated existing keys plus the fail-closed policy.</criterion>
    <criterion>Empty YAML, `{}`, and malformed custom YAML raise ConfigValidationError instead of returning None, raising an incidental KeyError, or silently loading defaults.</criterion>
    <criterion>An explicit empty mapping passed to a touched config-aware helper is not replaced through truthiness fallback.</criterion>
    <criterion>The fail-before-side-effect sentinel remains uncalled for invalid startup configuration.</criterion>
    <criterion>The focused validation command and Ruff command exit 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_validation.py &amp;&amp; python -m ruff check projects/spatial-pharma-dl/src/validation.py projects/spatial-pharma-dl/src/data.py projects/spatial-pharma-dl/tests/test_validation.py</automated>
  </verify>
  <done>The established YAML facade now fails once and early on invalid experiments while preserving valid notebook and runner dictionary behavior.</done>
</task>

</tasks>

<verification>
1. Run `python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_validation.py`.
2. Run `python -m ruff check projects/spatial-pharma-dl/src/validation.py projects/spatial-pharma-dl/src/data.py projects/spatial-pharma-dl/tests/test_validation.py`.
3. Import `src.validation` in a clean interpreter and assert no heavy scientific/model backend appears in `sys.modules`.
4. Confirm no notebook, documented CLI entry point, output filename, package discovery rule, or public export changed.
</verification>

<success_criteria>
- Invalid configuration sections, keys, types, values, and cross-field combinations are reported together at startup with actionable dotted paths.
- Valid configuration resolves to deterministic strict JSON and fresh mutable dictionaries without reordering cohort lists.
- The existing load_config entry point and all existing configuration keys remain compatible.
- The fail-closed partial-cohort flag is explicit and false by default.
- No high-severity T-01 through T-05 threat assigned to this plan remains open.
</success_criteria>

