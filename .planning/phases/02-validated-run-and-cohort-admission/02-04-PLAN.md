---
phase: 02-validated-run-and-cohort-admission
plan: "04"
type: execute
gap_closure: true
wave: 4
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - projects/spatial-pharma-dl/src/validation.py
  - projects/spatial-pharma-dl/src/foundation.py
  - projects/spatial-pharma-dl/src/eval.py
  - projects/spatial-pharma-dl/tests/test_validation.py
  - projects/spatial-pharma-dl/tests/test_empty_boundaries.py
autonomous: true
requirements:
  - VAL-01
  - VAL-03
  - VAL-04
must_haves:
  truths:
    - "G-01 / VAL-01 / D-01 / D-02 / T-02: Configuration validation accepts only exact safe built-in primitive and container types before invoking any value operation; hostile subclasses of int, float, str, list, tuple, dict or another Mapping, and pathlib.Path are rejected through one deterministic ConfigValidationError without running overridden bit_length, strip, repr, hash, comparison, iteration, length, lookup, or path-conversion methods."
    - "G-02 / T-01 / D-05: foundation_config, load_frozen_encoder, load_or_extract_slide_embeddings, save_benchmark_report, and every equivalent Phase 2 helper distinguish cfg is None from an explicit mapping; explicit empty or malformed mappings raise ConfigValidationError before cache-path, directory, device, encoder/model, dataframe, output, or writer seams and never reload repository defaults."
    - "VAL-03 / VAL-04 compatibility: Existing valid fully resolved configurations, admitted non-empty cohorts, function signatures, output names, lazy exports, and offline CPU behavior remain unchanged; this closure adds no cache durability, safe-format migration, identity repair, class-support policy, leakage/scaling/imputation policy, image science, or label-confidence behavior."
  artifacts:
    - path: projects/spatial-pharma-dl/src/validation.py
      provides: Total exact-type configuration admission and deterministic non-executing diagnostics for hostile primitive subclasses
      exports: [ConfigValidationError, ValidationIssue, ResolvedConfig, resolve_config]
    - path: projects/spatial-pharma-dl/src/foundation.py
      provides: Explicit-None configuration resolution before all foundation cache, device, and encoder seams
    - path: projects/spatial-pharma-dl/src/eval.py
      provides: Explicit-None report configuration resolution before output and writer seams
    - path: projects/spatial-pharma-dl/tests/test_validation.py
      provides: Offline hostile-subclass and deterministic aggregate regression evidence for G-01
    - path: projects/spatial-pharma-dl/tests/test_empty_boundaries.py
      provides: Offline explicit-empty and malformed-config forbidden-seam evidence for G-02
  key_links:
    - from: projects/spatial-pharma-dl/src/validation.py
      to: projects/spatial-pharma-dl/tests/test_validation.py
      via: adversarial subclasses prove exact-type gates reject values without invoking their overrides
    - from: projects/spatial-pharma-dl/src/foundation.py
      to: projects/spatial-pharma-dl/src/validation.py
      via: supplied mappings pass through resolve_config while only cfg is None may call load_config
    - from: projects/spatial-pharma-dl/src/eval.py
      to: projects/spatial-pharma-dl/src/validation.py
      via: save_benchmark_report validates supplied configuration before resolving an output path or constructing/writing a report
    - from: projects/spatial-pharma-dl/tests/test_empty_boundaries.py
      to: projects/spatial-pharma-dl/src/foundation.py
      via: forbidden cache, directory, device, model, encoder, output, and writer seams remain uncalled for explicit empty or malformed mappings
---

<objective>
Close the two independent Phase 2 verification gaps so configuration admission is total even for adversarial subclasses and every foundation/report helper preserves the difference between an omitted configuration and an explicitly invalid one.

Purpose: Restore the claimed aggregate startup boundary and prevent explicit empty or malformed configurations from being reinterpreted as defaults before expensive or output-producing work.
Output: Exact-type safe validation, consistent explicit-None configuration resolution at the four named public helpers and equivalent seams, adversarial offline regressions, and a green canonical fast gate.
</objective>

<execution_context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-validated-run-and-cohort-admission/02-CONTEXT.md
@.planning/phases/02-validated-run-and-cohort-admission/02-RESEARCH.md
@.planning/phases/02-validated-run-and-cohort-admission/02-VALIDATION.md
@.planning/phases/02-validated-run-and-cohort-admission/02-PATTERNS.md
@.planning/phases/02-validated-run-and-cohort-admission/02-01-PLAN.md
@.planning/phases/02-validated-run-and-cohort-admission/02-01-SUMMARY.md
@.planning/phases/02-validated-run-and-cohort-admission/02-02-PLAN.md
@.planning/phases/02-validated-run-and-cohort-admission/02-02-SUMMARY.md
@.planning/phases/02-validated-run-and-cohort-admission/02-03-PLAN.md
@.planning/phases/02-validated-run-and-cohort-admission/02-03-SUMMARY.md
@.planning/phases/02-validated-run-and-cohort-admission/02-REVIEW.md
@.planning/phases/02-validated-run-and-cohort-admission/02-REVIEW-FIX.md
@.planning/phases/02-validated-run-and-cohort-admission/02-VERIFICATION.md
</execution_context>

<threat_model>
  <threat id="T-01" severity="high" blocking="true">An explicit empty or malformed configuration is treated as absent and defaults are loaded, allowing cache, device, model, encoder, output-directory, dataframe, or writer work. Only `cfg is None` may load defaults; every supplied mapping must be resolved before any downstream seam.</threat>
  <threat id="T-02" severity="high" blocking="true">Validation invokes behavior overridden by a hostile subclass of an otherwise allowed primitive or container. Exact built-in type gates must run before strip, bit_length, finite/range comparison, length, iteration, lookup, hashing, sorting, representation, or path conversion.</threat>
  <threat id="T-03" severity="high" blocking="true">A defensive fix mutates cohort admission or silently drops configured slides. Preserve the existing final admitted sequence and strict/partial manifest policy; do not add a new filtering path.</threat>
  <threat id="T-04" severity="high" blocking="true">Diagnostics become nondeterministic or execute attacker-controlled repr/hash/comparison behavior. Render rejected subclasses from inert type metadata and schema paths only, and assert byte-identical exception text for semantically identical adversarial inputs.</threat>
  <threat id="T-05" severity="high" blocking="true">Gap closure weakens existing empty scientific-boundary guards. Keep StageValidationError ordering and all forbidden-seam tests green; configuration rejection must occur even earlier when cfg itself is malformed.</threat>
  <security_gate>Block completion if any hostile override is invoked, any explicit supplied mapping reloads defaults, any rejected configuration reaches a cache/device/model/output seam, diagnostics differ across equivalent adversarial inputs, or the canonical offline fast gate regresses.</security_gate>
</threat_model>

<tasks>

<task type="auto" id="2-04-01">
  <name>Make aggregate configuration validation total for hostile primitive subclasses</name>
  <files>projects/spatial-pharma-dl/src/validation.py, projects/spatial-pharma-dl/tests/test_validation.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/validation.py</file>
    <file>projects/spatial-pharma-dl/tests/test_validation.py</file>
    <file>projects/spatial-pharma-dl/configs/default.yaml</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-01-PLAN.md</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-REVIEW-FIX.md</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-VERIFICATION.md</file>
  </read_first>
  <action>Refactor the validation entry, optional-default merge, `_mapping`, `_string`, `_boolean`, `_number`, `_choice`, `_string_list`, dynamic-map traversal, and `_validate_json_tree` so no operation is performed on a caller value until its type is admitted by an exact built-in check. Admit exact `dict`, `list`, `tuple`, `str`, `bool`, `int`, `float`, `None`, and the platform's exact concrete `Path` type only at the layers where the existing contract permits them; reject subclasses and arbitrary `Mapping`/sequence implementations as unsupported values rather than copying, coercing, or traversing them. Run `bit_length`, `strip`, `math.isfinite`, numeric comparisons, `len`, iteration, membership, lookup, sorting, and `as_posix` only after the corresponding exact-type gate. Ensure `_merge_optional_defaults` never calls `dict()`, `deepcopy`, membership, iteration, or lookup on an untrusted mapping subclass; trusted defaults may still be copied. Preserve aggregate schema traversal for all safe plain-dict content and route every rejected subclass into `ValidationIssue` plus one `ConfigValidationError`. Keep `_safe_received` and invalid-key ordering non-executing: never call caller `repr`, `str`, `hash`, rich comparison, or subclass methods, and use bounded inert type labels plus deterministic schema paths/tokens. Extend `test_validation.py` with adversarial subclasses of int, float, str, list, tuple, dict/Mapping, and the concrete Path type whose relevant overrides (`bit_length`, `strip`, `__repr__`, `__hash__`, rich comparisons, `__len__`, `__iter__`, `__getitem__`, `as_posix`) raise and increment sentinels. Place them at root, scalar, sequence, dynamic-map, and canonical-tree positions; assert all sentinels remain zero, one ConfigValidationError contains all safe-to-discover dotted paths in schema order, diagnostics contain no attacker text, and equivalent reversed plain mappings yield byte-identical exception text. Retain the existing oversized built-in integer and invalid primitive-key behavior, canonical JSON ordering, fresh `to_dict()`, and lightweight-import contract.</action>
  <acceptance_criteria>
    <criterion>`resolve_config` rejects a dict/Mapping subclass at `config` through ConfigValidationError before optional-default merging or iteration invokes any override.</criterion>
    <criterion>Hostile subclasses of int, float, str, list, tuple, nested dict/Mapping, and concrete Path placed in a plain configuration tree all produce ConfigValidationError; their bit_length, strip, repr, hash, comparison, len, iteration, lookup, and path-conversion sentinels remain zero.</criterion>
    <criterion>Multiple hostile subclass values inside one exact plain dict are accumulated into one exception in deterministic schema-path order wherever safe traversal can continue.</criterion>
    <criterion>Exception rendering contains bounded inert type labels and corrective guidance, never attacker-controlled representation text, and equivalent adversarial plain mappings produce byte-identical exception text.</criterion>
    <criterion>Exact built-in valid values still resolve to the same canonical JSON; oversized exact integers, non-finite exact floats, invalid exact primitive keys, list order, and fresh mutable compatibility views retain their existing behavior.</criterion>
    <criterion>The focused hostile validation suite and scoped Ruff command exit 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_validation.py &amp;&amp; python -m ruff check projects/spatial-pharma-dl/src/validation.py projects/spatial-pharma-dl/tests/test_validation.py</automated>
  </verify>
  <done>G-01 is closed: validation is a total, deterministic, non-executing boundary for both ordinary invalid values and hostile subclasses of otherwise allowed types.</done>
</task>

<task type="auto" id="2-04-02">
  <name>Reject explicit invalid configs before foundation and report side effects</name>
  <files>projects/spatial-pharma-dl/src/foundation.py, projects/spatial-pharma-dl/src/eval.py, projects/spatial-pharma-dl/tests/test_empty_boundaries.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/validation.py</file>
    <file>projects/spatial-pharma-dl/src/foundation.py</file>
    <file>projects/spatial-pharma-dl/src/eval.py</file>
    <file>projects/spatial-pharma-dl/src/data.py</file>
    <file>projects/spatial-pharma-dl/tests/test_empty_boundaries.py</file>
    <file>projects/spatial-pharma-dl/tests/test_foundation.py</file>
    <file>projects/spatial-pharma-dl/tests/test_cohort_admission.py</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-PATTERNS.md</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-VERIFICATION.md</file>
  </read_first>
  <action>Replace `cfg = cfg or load_config()` in `foundation_config`, `load_frozen_encoder`, `load_or_extract_slide_embeddings`, and `eval.save_benchmark_report` with one consistent explicit-None resolution pattern: call `load_config()` only when `cfg is None`; otherwise pass the supplied mapping through `resolve_config(cfg).to_dict()` before reading any key or calling any helper. Audit all Phase 2 config-aware helpers with `rg` and apply the same rule to any equivalent truthiness fallback found in the touched foundation/report call chain. Order checks so explicit `{}` and malformed mappings raise ConfigValidationError before `foundation_model_spec`, `_embedding_cache_path`, `pharma_processed_dir`, `pharma_outputs_dir`, `Path.exists`, NumPy cache loading/saving, `resolve_device`, diagnostic printing, timm/transformers loading, encoder construction, `load_slide_patches`, pandas DataFrame construction, path derivation, or `to_csv`. Avoid a second default reload in nested calls by passing the already resolved plain dictionary through `foundation_config`, `foundation_model_spec`, cache-path, and encoder helpers. Preserve `cfg=None` behavior, existing public signatures, default foundation values, valid encoder/cache/report behavior, report filename `benchmark_report_{experiment}.csv`, and current return types. Add table-driven offline regressions in `test_empty_boundaries.py` for explicit `{}` and representative malformed mappings at all four named helpers. Monkeypatch `load_config` plus every relevant cache, directory, device, model/encoder, dataframe, output, and writer seam to raise if called; assert ConfigValidationError is raised first and every forbidden seam remains untouched. Add valid-full-config compatibility cases proving omitted cfg still uses the default facade and supplied resolved cfg reaches the existing behavior without reloading defaults. Do not add cache fingerprints, atomic writes, safe serialization migrations, model-download behavior, fold class policy, or leakage/scaling/imputation changes.</action>
  <acceptance_criteria>
    <criterion>`rg` finds no `cfg = cfg or load_config()` or equivalent truthiness fallback in `src/foundation.py`, `src/eval.py`, or another touched Phase 2 helper.</criterion>
    <criterion>`foundation_config({})`, `load_frozen_encoder({})`, `load_or_extract_slide_embeddings(..., cfg={})`, and `save_benchmark_report(..., cfg={})` each raise ConfigValidationError and never call load_config.</criterion>
    <criterion>Representative non-empty malformed mappings fail through the same aggregate ConfigValidationError before cache-path, directory, exists/load/save, device, print, encoder/model, slide-patch, dataframe, output-path, or writer seams.</criterion>
    <criterion>Forbidden-seam tests prove no cache directory is created, no model or encoder is loaded, and no report path or file is produced for explicit invalid mappings, including when a report path argument is supplied.</criterion>
    <criterion>`cfg=None` still loads the validated repository default once, while a supplied complete valid plain configuration is used without default reload and preserves existing foundation/cache/report signatures, filenames, and return schemas.</criterion>
    <criterion>The focused Phase 2/foundation tests, scoped Ruff, and canonical `python scripts/verify.py fast` gate exit 0 offline on CPU.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_validation.py projects/spatial-pharma-dl/tests/test_empty_boundaries.py projects/spatial-pharma-dl/tests/test_foundation.py projects/spatial-pharma-dl/tests/test_cohort_admission.py &amp;&amp; python -m ruff check projects/spatial-pharma-dl/src/validation.py projects/spatial-pharma-dl/src/foundation.py projects/spatial-pharma-dl/src/eval.py projects/spatial-pharma-dl/tests/test_validation.py projects/spatial-pharma-dl/tests/test_empty_boundaries.py &amp;&amp; python scripts/verify.py fast</automated>
  </verify>
  <done>G-02 is closed: omitted configuration alone may load defaults, while every explicit invalid foundation/report configuration fails at the validation boundary before observable side effects.</done>
</task>

</tasks>

<verification>
1. Run `python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_validation.py` and require all hostile-subclass sentinels to remain zero while ConfigValidationError diagnostics remain deterministic.
2. Run `python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_empty_boundaries.py projects/spatial-pharma-dl/tests/test_foundation.py projects/spatial-pharma-dl/tests/test_cohort_admission.py` and require every explicit-invalid-config forbidden seam to remain uncalled.
3. Run `rg -n "cfg\\s*=\\s*cfg\\s+or\\s+load_config\\(\\)|cfg\\s+or\\s+load_config\\(\\)" projects/spatial-pharma-dl/src` and require no match in the Phase 2 foundation/report helpers.
4. Run scoped Ruff over all five modified files, then run `python scripts/verify.py fast`; require Ruff first, the complete offline suite second, exit 0, and no network/model download.
5. Review the diff against `02-VERIFICATION.md`: G-01 and G-02 must each have direct adversarial behavioral evidence, while Phase 3 identity, Phase 4-5 artifact guarantees, Phase 6 class policy, Phase 7 leakage/scaling/imputation, Phase 8 image behavior, and Phase 9 label behavior remain untouched.
</verification>

<success_criteria>
- G-01 is closed by exact-type admission that never invokes hostile primitive/container/path subclass behavior and always reports safe discoverable defects through deterministic ConfigValidationError evidence.
- G-02 is closed at foundation_config, load_frozen_encoder, load_or_extract_slide_embeddings, save_benchmark_report, and equivalent helpers: explicit empty or malformed mappings never reload defaults or reach cache/model/output side effects.
- VAL-01 aggregate startup validation, VAL-03 early empty-boundary behavior, and VAL-04 admitted-cohort behavior all retain direct offline regression evidence.
- Valid complete configurations preserve the established notebook, CLI, function, output-name, and public-import contracts.
- The canonical fast CPU/offline gate passes and no later-phase behavior is implemented early.
</success_criteria>
