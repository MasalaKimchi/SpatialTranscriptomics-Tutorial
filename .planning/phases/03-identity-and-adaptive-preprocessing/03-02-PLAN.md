---
phase: 03-identity-and-adaptive-preprocessing
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - projects/spatial-pharma-dl/src/validation.py
  - projects/spatial-pharma-dl/src/data.py
  - projects/spatial-pharma-dl/scripts/run_pipeline.py
  - projects/spatial-pharma-dl/tests/conftest.py
  - projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py
autonomous: true
requirements:
  - VAL-05
must_haves:
  truths:
    - "D-03: Actual post-QC spot/gene counts and actual selected-HVG count deterministically accept or cap HVG, PCA, neighbor, and neighbor-PC dimensions using explicit formulas/reason codes, or raise structured scientific nonviability before PCA, neighbors, UMAP, Leiden, or save work."
    - "D-04: AnnData and additive admitted-order run provenance expose the same JSON-safe input/stage counts, exclusions, requested/resolved values, and reason codes with byte-stable canonical serialization and H5AD round-trip stability."
    - "D-05: preprocess_slide keeps its public signature, existing QC/normalize/log/HVG/scale/PCA/neighbors/UMAP/Leiden order and ordinary analysis fields, while cohort-manifest-v1, config keys, output names, notebooks, CLI flags, and imports remain unchanged."
  artifacts:
    - path: projects/spatial-pharma-dl/src/validation.py
      provides: Import-light immutable preprocessing resolution/provenance values and two-stage scientific viability resolvers
      exports: [PreprocessingValidationError, PreprocessingResolution, resolve_post_qc_preprocessing, finalize_preprocessing_resolution, PreprocessingManifest]
    - path: projects/spatial-pharma-dl/src/data.py
      provides: Post-filter count capture, exact resolved Scanpy calls, and AnnData preprocessing metadata publication
    - path: projects/spatial-pharma-dl/scripts/run_pipeline.py
      provides: Additive preprocessing_manifest.json assembled from final admitted slides in admitted order
    - path: projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py
      provides: Offline cap/minimum, fake-Scanpy ordering, canonical provenance, H5AD, and runner-publication evidence
  key_links:
    - from: projects/spatial-pharma-dl/src/data.py
      to: projects/spatial-pharma-dl/src/validation.py
      via: observed counts before and after HVG selection feed the pure two-stage resolver
    - from: projects/spatial-pharma-dl/src/data.py
      to: AnnData.uns[spatial_pharma_preprocessing]
      via: the finalized resolution is converted to a fresh exact-built-in primitive tree before return/save
    - from: projects/spatial-pharma-dl/scripts/run_pipeline.py
      to: processed AnnData preprocessing metadata
      via: final_admitted.slide_ids order drives one canonical sibling manifest before label/model stages
---

<objective>
As a maintainer, I want preprocessing dimensions derived from the slide that actually survived QC and recorded exactly where results travel so that Scanpy never receives illegal hidden dimensions and every analysis is reconstructable.

Purpose: Deliver VAL-05 on top of trusted Phase 3 spot identity without changing the tutorial's successful workflow.
Output: Pure two-stage resolution, structured nonviability, resolved Scanpy orchestration, AnnData metadata, run provenance, and offline evidence.
</objective>

<execution_context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-identity-and-adaptive-preprocessing/03-CONTEXT.md
@.planning/phases/03-identity-and-adaptive-preprocessing/03-RESEARCH.md
@.planning/phases/03-identity-and-adaptive-preprocessing/03-VALIDATION.md
@.planning/phases/03-identity-and-adaptive-preprocessing/03-PATTERNS.md
@.planning/phases/03-identity-and-adaptive-preprocessing/03-01-PLAN.md
@.planning/phases/02-validated-run-and-cohort-admission/02-04-SUMMARY.md
</execution_context>

<threat_model>
  <threat id="T-04" severity="high" blocking="true">Fixed configured dimensions become illegal after filtering or actual HVG selection. Resolve twice from observed counts using conservative rank and neighbor limits and explicit scientific minima before graph work.</threat>
  <threat id="T-05" severity="high" blocking="true">Scanpy implicitly adjusts parameters or output metadata omits the actual analysis. Pass finalized values exactly once and publish canonical requested/resolved/count/exclusion/reason evidence in AnnData and run provenance.</threat>
  <security_gate>Block completion while an impossible state can reach PCA/graph/save work, a resolved call relies on retry/implicit adjustment, or provenance contains non-primitive/unstable values.</security_gate>
</threat_model>

<tasks>

<task type="auto" id="3-02-01">
  <name>Build Wave 0 dimension matrices and the pure two-stage resolver</name>
  <files>projects/spatial-pharma-dl/tests/conftest.py, projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py, projects/spatial-pharma-dl/src/validation.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/validation.py</file>
    <file>projects/spatial-pharma-dl/src/data.py</file>
    <file>projects/spatial-pharma-dl/configs/default.yaml</file>
    <file>projects/spatial-pharma-dl/tests/conftest.py</file>
    <file>projects/spatial-pharma-dl/tests/test_validation.py</file>
    <file>.planning/phases/03-identity-and-adaptive-preprocessing/03-RESEARCH.md</file>
    <file>.planning/phases/03-identity-and-adaptive-preprocessing/03-VALIDATION.md</file>
    <file>.planning/phases/03-identity-and-adaptive-preprocessing/03-PATTERNS.md</file>
  </read_first>
  <action>Extend the existing synthetic AnnData factory through parameters or derived fresh copies for viable, heavily filtered, and scientifically nonviable spot/gene cardinalities; do not add a competing fixture convention. Create `test_adaptive_preprocessing.py` with `pytestmark = pytest.mark.offline` before implementation. In import-light `src.validation`, define `PreprocessingValidationError(PharmaValidationError)` exposing stable primitive `stage`, `reason_code`, `counts`, `requested`, and `guidance`; frozen slotted `PreprocessingResolution` backed by canonical JSON/fresh `to_dict()`; and pure two-stage helpers. The post-QC stage must require at least three spots and two genes, compute `resolved_hvg_call=min(requested_hvg, post_qc_genes)`, and record accepted/capped reason. The final stage must use actual selected HVGs, require at least two, compute `pca_rank_limit=min(post_qc_spots, actual_hvgs)-1`, `resolved_pcs=min(requested_pcs,pca_rank_limit)`, `resolved_neighbors=min(requested_neighbors,post_qc_spots-1)`, and `resolved_graph_pcs=min(requested_graph_pcs,resolved_pcs)`, while requiring at least one PCA component and at least two legal neighbors. Record stable accepted/capped reason codes for every parameter and strict canonical JSON using sorted keys, compact separators, UTF-8 characters, and `allow_nan=False`. Reject non-exact primitive counts/requests before arithmetic; do not import pandas, NumPy, AnnData, or Scanpy at validation module scope.</action>
  <acceptance_criteria>
    <criterion>Accepted requests pass through unchanged with `requested_value_accepted`; oversized HVG/PCA/neighbors/graph-PC values cap independently and jointly to the documented formulas and reason codes.</criterion>
    <criterion>Zero/one/two post-QC spots, zero/one genes, zero/one actual HVGs, no legal PCA, and fewer than two legal neighbors raise structured errors with stage, reason, counts, request, and guidance.</criterion>
    <criterion>Actual HVGs, not only the requested or HVG-call count, determine final PCA rank.</criterion>
    <criterion>Repeated semantic resolution has byte-identical canonical JSON, returns mutation-isolated exact-built-in trees, and imports no heavy scientific/model library.</criterion>
    <criterion>The resolver-focused Wave 0 command exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py -k "resolve or cap or nonviable or canonical"</automated>
  </verify>
  <done>Every viable observed cardinality produces one legal deterministic parameter set, and every nonviable state fails before Scanpy graph work.</done>
</task>

<task type="auto" id="3-02-02">
  <name>Integrate observed counts and finalized dimensions into preprocess_slide</name>
  <files>projects/spatial-pharma-dl/src/data.py, projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/data.py</file>
    <file>projects/spatial-pharma-dl/src/validation.py</file>
    <file>projects/spatial-pharma-dl/src/identity.py</file>
    <file>projects/spatial-pharma-dl/tests/test_synthetic_anndata.py</file>
    <file>projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py</file>
    <file>.planning/phases/03-identity-and-adaptive-preprocessing/03-PATTERNS.md</file>
  </read_first>
  <action>Keep `preprocess_slide(adata, sample_id, cfg=None, seed=None)` unchanged. Resolve supplied configuration and validate source compound identity before importing/calling Scanpy, copying or mutating AnnData, seeding, or other scientific work; only then import Scanpy and copy the AnnData. Capture exact integer counts at input, after `filter_cells`, after `filter_genes`, after mitochondrial filtering, and after HVG selection; derive nonnegative exclusions that reconcile each transition. Call the post-QC resolver before normalize/HVG-dependent graph work, pass its resolved HVG value to `highly_variable_genes`, count the actual boolean HVG mask, finalize PCA/neighbors/graph-PC values, and pass those exact values once to `sc.pp.pca` and `sc.pp.neighbors`. Preserve the current QC -> normalize/log -> HVG -> scale -> PCA -> neighbors -> UMAP -> Leiden order, seed use, layers/raw/X_pca/pca/clusters fields, and successful return shape. Attach a fresh exact-built-in tree under `adata.uns["spatial_pharma_preprocessing"]` containing schema version, slide ID, all counts/exclusions, requested/resolved parameters, and reasons. Add fake/call-recording Scanpy tests that assert values and order, forbid Scanpy import/calls, copying, seeding, PCA/neighbors/UMAP/Leiden/save after the corresponding invalid states, prove no catch/retry occurs, and use real tiny AnnData/H5AD round trips for JSON-safe metadata and established fields.</action>
  <acceptance_criteria>
    <criterion>Counts and exclusions are captured at the specified boundaries, are built-in integers, remain nonnegative, and reconcile with observed AnnData shapes.</criterion>
    <criterion>The exact resolved values are passed once to HVG, PCA, and neighbors, with actual selected HVGs determining final dimensions.</criterion>
    <criterion>Nonviable states fail before PCA, neighbors, UMAP, Leiden, or output work; there is no solver switch, retry, inferred fallback, or silent skipped stage.</criterion>
    <criterion>`spatial_pharma_preprocessing` is JSON-safe with `allow_nan=False`, byte-stable through canonicalization, and equal after H5AD round trip.</criterion>
    <criterion>Viable ordinary slides retain counts/raw/X_pca/pca/clusters and existing public behavior.</criterion>
    <criterion>The orchestration, H5AD, and scoped Ruff commands exit 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py -k "orchestration or scanpy or guard or h5ad or round_trip" &amp;&amp; python -m ruff check projects/spatial-pharma-dl/src/validation.py projects/spatial-pharma-dl/src/data.py projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py</automated>
  </verify>
  <done>The production preprocessing sequence consumes only legal finalized dimensions and carries exact observed analysis metadata in its returned AnnData.</done>
</task>

<task type="auto" id="3-02-03">
  <name>Publish additive admitted-order preprocessing run provenance</name>
  <files>projects/spatial-pharma-dl/src/validation.py, projects/spatial-pharma-dl/scripts/run_pipeline.py, projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/validation.py</file>
    <file>projects/spatial-pharma-dl/src/data.py</file>
    <file>projects/spatial-pharma-dl/scripts/run_pipeline.py</file>
    <file>projects/spatial-pharma-dl/tests/test_cohort_admission.py</file>
    <file>projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py</file>
    <file>.planning/phases/02-validated-run-and-cohort-admission/02-02-SUMMARY.md</file>
  </read_first>
  <action>Add a frozen canonical `PreprocessingManifest` value in `src.validation` with schema version `spatial-pharma-preprocessing-manifest-v1`, final admitted slide order, and one validated primitive preprocessing record per slide. Validate the entire candidate tree recursively using exact built-in dict/list/string/bool/int/finite-float/null admission (including exact string mapping keys and duplicate slide detection) before canonical JSON, so malformed or hostile values cannot reach representation, hashing, comparison, coercion, or serialization hooks. In `run_pipeline.py`, after final cohort admission and stage loading, read each admitted processed AnnData's `uns["spatial_pharma_preprocessing"]`, validate complete exact slide membership/order and schema, assemble canonical bytes in `final_admitted.slide_ids` order, and only then publish additive `preprocessing_manifest.json` alongside the unchanged `cohort_manifest.json` before label, patch, model, or report stages. Both normal and train-only flows must use the same assembly; missing/malformed/duplicate/mismatched metadata must fail before any preprocessing-manifest write or downstream label/patch/model/report effect. Keep ordinary `Path.write_text` because atomic publication/checksums/fingerprints are Phase 4. Add stubbed runner tests under `tmp_path` proving admitted order, byte identity across repeated runs, AnnData/run content equality, hostile/malformed values rejected before serialization, no preprocessing-manifest publication before complete validation, no downstream label/model call after failure, and exact `cohort-manifest-v1` bytes/schema remain unchanged.</action>
  <acceptance_criteria>
    <criterion>Normal and train-only runs publish the same additive manifest schema from final admitted slide order and complete validated per-slide metadata.</criterion>
    <criterion>AnnData and run-level records contain semantically identical counts, exclusions, requested/resolved values, and reasons using only exact JSON primitives.</criterion>
    <criterion>Repeated equivalent input produces byte-identical preprocessing manifest content.</criterion>
    <criterion>Missing, malformed, hostile, duplicate, wrong-slide, or incomplete preprocessing metadata reaches no preprocessing-manifest write, label, patch, model, or report sentinel.</criterion>
    <criterion>`cohort_manifest.json` and `cohort-manifest-v1` remain byte/schema compatible; no atomicity, checksum, completion, or fingerprint claim is added.</criterion>
    <criterion>The provenance and cohort regression commands exit 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py -k "provenance or manifest or admitted or pipeline or canonical" projects/spatial-pharma-dl/tests/test_cohort_admission.py &amp;&amp; python -m ruff check projects/spatial-pharma-dl/src/validation.py projects/spatial-pharma-dl/scripts/run_pipeline.py projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py</automated>
  </verify>
  <done>Every admitted run exposes byte-stable per-slide preprocessing facts without changing the Phase 2 cohort contract or claiming later artifact durability.</done>
</task>

</tasks>

<verification>
1. Run `python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_adaptive_preprocessing.py`.
2. Run affected validation, synthetic AnnData, cohort-admission, and identity modules.
3. Run Ruff over all modified files.
4. Inspect production calls to confirm finalized HVG/PCA/neighbor values are passed exactly once and no exception retry, solver switch, graph-stage skip, or output-before-validation exists.
5. Confirm cohort-manifest-v1, config keys, public signatures, notebook/CLI order, existing AnnData analysis keys, and intended output names remain stable except the additive preprocessing manifest/metadata.
</verification>

<success_criteria>
- Actual post-QC and selected-HVG counts deterministically produce legal HVG/PCA/neighbor dimensions or structured scientific failure.
- Requested/resolved values, counts, exclusions, and reason codes survive AnnData/H5AD and admitted-order run provenance as canonical JSON-safe state.
- Invalid dimensions or provenance stop before graph, writer, label, patch, model, and report effects.
- Successful tutorial preprocessing fields and all public surfaces remain compatible.
- No high-severity T-04 or T-05 control remains open.
</success_criteria>
