---
phase: 03-identity-and-adaptive-preprocessing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/spatial-pharma-dl/src/identity.py
  - projects/spatial-pharma-dl/src/labels.py
  - projects/spatial-pharma-dl/src/patches.py
  - projects/spatial-pharma-dl/src/train.py
  - projects/spatial-pharma-dl/src/foundation.py
  - projects/spatial-pharma-dl/tests/conftest.py
  - projects/spatial-pharma-dl/tests/test_identity_alignment.py
autonomous: true
requirements:
  - VAL-02
must_haves:
  truths:
    - "D-01: Both slide_id and spot_id are exact built-in non-blank strings, and missing columns, invalid values, duplicates, wrong-slide rows, unmatched keys, cross-slide keys, and value/metadata cardinality mismatches fail with deterministic structured evidence before pandas hashing, merge, NumPy indexing, model, encoder, or cache-write work."
    - "D-02: Complete shuffled labels and patch/cache metadata align one-to-one in metadata order, preserving every target and provenance column together and exposing _label_source_row plus _patch_source_row without overwriting caller columns."
    - "D-05: align_labels_with_patches keeps its public DataFrame return contract, ordinary patch and foundation embedding consumers share one compound-key implementation, and successful array shapes, cache names, model interfaces, CLI entry points, notebooks, and public imports remain unchanged."
  artifacts:
    - path: projects/spatial-pharma-dl/src/identity.py
      provides: Structured exact-key admission, AnnData source-identity guard, and complete metadata-order one-to-one alignment
      exports: [IdentityIssue, IdentityValidationError, validate_anndata_spot_identity, align_labels_with_metadata]
    - path: projects/spatial-pharma-dl/tests/test_identity_alignment.py
      provides: Offline malformed/hostile identity, deterministic diagnostic, shuffled success, cardinality, and consumer-boundary evidence
    - path: projects/spatial-pharma-dl/src/train.py
      provides: Ordinary patch-array consumer indexed only by validated patch source rows
    - path: projects/spatial-pharma-dl/src/foundation.py
      provides: Foundation cache hit and extraction paths routed through the same complete compound-key contract
  key_links:
    - from: projects/spatial-pharma-dl/src/labels.py
      to: projects/spatial-pharma-dl/src/identity.py
      via: the established align_labels_with_patches facade delegates to align_labels_with_metadata
    - from: projects/spatial-pharma-dl/src/patches.py
      to: projects/spatial-pharma-dl/src/identity.py
      via: AnnData identity is admitted before coordinate iteration and patch array construction
    - from: projects/spatial-pharma-dl/src/train.py
      to: projects/spatial-pharma-dl/src/identity.py
      via: value_row_count and expected_slide_id are validated before indexing patches by _patch_source_row
    - from: projects/spatial-pharma-dl/src/foundation.py
      to: projects/spatial-pharma-dl/src/identity.py
      via: cached spot IDs are represented as slide-scoped metadata and aligned exactly like ordinary patch metadata
---

<objective>
As a maintainer, I want one strict compound-identity and alignment contract shared by every patch and embedding consumer so that no spot can be silently dropped, duplicated, coerced, or associated with the wrong slide.

Purpose: Deliver the complete VAL-02 vertical slice before any Phase 3 preprocessing work depends on spot identity.
Output: A pandas-aware identity module, guarded label/patch producers, ordinary and foundation consumer wiring, and adversarial offline evidence.
</objective>

<execution_context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-identity-and-adaptive-preprocessing/03-CONTEXT.md
@.planning/phases/03-identity-and-adaptive-preprocessing/03-RESEARCH.md
@.planning/phases/03-identity-and-adaptive-preprocessing/03-VALIDATION.md
@.planning/phases/03-identity-and-adaptive-preprocessing/03-PATTERNS.md
@.planning/phases/02-validated-run-and-cohort-admission/02-03-SUMMARY.md
@.planning/phases/02-validated-run-and-cohort-admission/02-04-SUMMARY.md
</execution_context>

<threat_model>
  <threat id="T-01" severity="high" blocking="true">Caller-controlled key objects execute hashing, comparison, coercion, string, or representation methods during validation. Exact built-in type admission and inert ordinal/type diagnostics must precede pandas or Python key operations.</threat>
  <threat id="T-02" severity="high" blocking="true">An inner join silently loses or multiplies rows, or a repeated barcode aligns across slides. Both complete compound-key sets must be unique and equal before a metadata-left one-to-one merge.</threat>
  <threat id="T-03" severity="high" blocking="true">Patch and foundation paths diverge or accept array/metadata mismatch. Both must call one shared alignment contract with value cardinality and expected-slide checks before indexing, model, encoder, or write work.</threat>
  <security_gate>Block completion while any T-01 through T-03 control is bypassable, diagnostics are nondeterministic, or a production consumer retains a spot_id-only/coercing alignment path.</security_gate>
</threat_model>

<tasks>

<task type="auto" id="3-01-01">
  <name>Build Wave 0 adversaries and the exact compound-key admission contract</name>
  <files>projects/spatial-pharma-dl/tests/conftest.py, projects/spatial-pharma-dl/tests/test_identity_alignment.py, projects/spatial-pharma-dl/src/identity.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/validation.py</file>
    <file>projects/spatial-pharma-dl/src/labels.py</file>
    <file>projects/spatial-pharma-dl/src/patches.py</file>
    <file>projects/spatial-pharma-dl/tests/conftest.py</file>
    <file>projects/spatial-pharma-dl/tests/test_fixture_contracts.py</file>
    <file>.planning/phases/03-identity-and-adaptive-preprocessing/03-RESEARCH.md</file>
    <file>.planning/phases/03-identity-and-adaptive-preprocessing/03-PATTERNS.md</file>
    <file>.planning/phases/03-identity-and-adaptive-preprocessing/03-VALIDATION.md</file>
  </read_first>
  <action>Extend the existing fresh `key_adversary_factory` with shuffled-complete labels and metadata, each missing required column, null/blank/whitespace keys, exact-type violations and hostile string subclasses on both sides, duplicate metadata keys, wrong-slide metadata, value-row mismatch, and collisions with `_label_source_row`/`_patch_source_row`. Create `test_identity_alignment.py` with `pytestmark = pytest.mark.offline` before production code and characterize every VAL-02 category. Add pandas-aware `src.identity` without importing Scanpy, torch, or model libraries. Define frozen slotted `IdentityIssue(code, side, count, sample_keys, sample_rows)` and `IdentityValidationError(PharmaValidationError)` with immutable ordered issues and bounded inert text. Validate required columns and reserved-name collisions first; inspect cells by ordinal and require `type(value) is str` before `strip`, tuple creation, hashing, sorting, duplicated, merge, or indexing; report rejected cells only by side/column/row ordinal/exact type label. Once exact strings are admitted, reject blanks, duplicates, expected-slide violations, label-only/metadata-only keys, cross-slide matches, and optional `value_row_count != len(metadata)` using a fixed category order, total counts, and at most five lexicographically sorted safe key samples. Do not coerce, trim, fill, drop, infer from indexes, or repair input.</action>
  <acceptance_criteria>
    <criterion>`src.identity` exports the named structured error/issue records and key-validation helpers while retaining `PharmaValidationError`/`ValueError` compatibility.</criterion>
    <criterion>Missing columns, reserved collisions, null/wrong-type/blank values, duplicates, wrong-slide rows, unmatched/cross-slide keys, and cardinality mismatch each produce a structured deterministic issue with a total count and bounded safe sample.</criterion>
    <criterion>Hostile values with raising repr, str, hash, equality, comparison, strip, and iteration hooks execute none of those hooks before rejection.</criterion>
    <criterion>Reordered construction of equivalent defects yields identical issue ordering and exception text after safe-key sorting.</criterion>
    <criterion>The focused Wave 0 command exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_identity_alignment.py -k "key or hostile or duplicate or mismatch or reserved"</automated>
  </verify>
  <done>Every malformed compound identity is rejected deterministically before any caller-controlled key operation or data alignment.</done>
</task>

<task type="auto" id="3-01-02">
  <name>Implement complete metadata-order alignment and guard label and patch producers</name>
  <files>projects/spatial-pharma-dl/src/identity.py, projects/spatial-pharma-dl/src/labels.py, projects/spatial-pharma-dl/src/patches.py, projects/spatial-pharma-dl/tests/test_identity_alignment.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/identity.py</file>
    <file>projects/spatial-pharma-dl/src/labels.py</file>
    <file>projects/spatial-pharma-dl/src/patches.py</file>
    <file>projects/spatial-pharma-dl/tests/test_synthetic_anndata.py</file>
    <file>projects/spatial-pharma-dl/tests/test_identity_alignment.py</file>
    <file>.planning/phases/03-identity-and-adaptive-preprocessing/03-PATTERNS.md</file>
  </read_first>
  <action>Implement `align_labels_with_metadata(labels, metadata, *, stage, expected_slide_id=None, value_row_count=None)` so it validates the exact built-in/nonblank `stage` and optional `expected_slide_id`, then validates the complete label table before selecting the expected-slide subset, validates metadata entirely belongs to that slide, proves uniqueness and exact set equality, adds source ordinals, and only then performs a metadata-left `merge(... how="left", validate="one_to_one", sort=False, indicator=True)`. Treat a merge failure after proof as an internal invariant error, drop only the indicator, retain canonical keys and both source ordinals, preserve metadata order, and keep every target/provenance column paired. Implement `validate_anndata_spot_identity(adata, slide_id, *, stage)` by admitting the exact built-in/nonblank slide/stage parameters and obs-name-derived spot IDs before row construction. Route `labels.align_labels_with_patches` through the shared aligner without changing its two-argument signature or DataFrame return type. In `build_labels_for_slide`, call the AnnData guard immediately after `load_slide` and before marker lookup, module scoring, `rank_genes_groups`, `to_df`, sorting, or label-row construction; in `_extract_spot_patches`, call it before coordinates, image access, patch transforms, or `np.stack`. Extend tests for shuffled complete success, repeated barcode strings on different valid slides, preserved target/provenance pairing, exact source ordinals, reserved-column rejection, and forbidden marker/module/ranking/to-DataFrame/merge/coordinate/stack seams after invalid identity.</action>
  <acceptance_criteria>
    <criterion>Independently shuffled complete tables return exactly metadata-order rows with unchanged compound keys, target/provenance pairing, and inspectable source ordinals.</criterion>
    <criterion>Other valid cohort slide rows are not classified as extras, while a requested spot present only under another slide produces a cross-slide issue.</criterion>
    <criterion>`align_labels_with_patches(labels, meta)` retains its established public signature and now rejects incomplete or multiplying joins rather than returning a subset.</criterion>
    <criterion>Invalid AnnData identity fails before label marker/module/ranking/expression work and before coordinate extraction, image/patch transformation, row construction, or NumPy stacking.</criterion>
    <criterion>No patch format, border geometry, stain-science, cache fingerprint, or atomic-write change is introduced.</criterion>
    <criterion>The focused alignment command and scoped Ruff command exit 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_identity_alignment.py -k "shuffle or order or source or anndata or producer" &amp;&amp; python -m ruff check projects/spatial-pharma-dl/src/identity.py projects/spatial-pharma-dl/src/labels.py projects/spatial-pharma-dl/src/patches.py projects/spatial-pharma-dl/tests/test_identity_alignment.py</automated>
  </verify>
  <done>Label and patch producers establish trustworthy source identity, and the compatibility aligner preserves complete metadata order without silent row loss.</done>
</task>

<task type="auto" id="3-01-03">
  <name>Route ordinary patch and foundation embedding consumers through one alignment contract</name>
  <files>projects/spatial-pharma-dl/src/train.py, projects/spatial-pharma-dl/src/foundation.py, projects/spatial-pharma-dl/tests/test_identity_alignment.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/identity.py</file>
    <file>projects/spatial-pharma-dl/src/train.py</file>
    <file>projects/spatial-pharma-dl/src/foundation.py</file>
    <file>projects/spatial-pharma-dl/tests/test_foundation.py</file>
    <file>projects/spatial-pharma-dl/tests/test_empty_boundaries.py</file>
    <file>projects/spatial-pharma-dl/tests/test_identity_alignment.py</file>
    <file>.planning/phases/03-identity-and-adaptive-preprocessing/03-VALIDATION.md</file>
  </read_first>
  <action>In `train.load_slide_patches`, pass the full cohort label table, patch metadata, `expected_slide_id`, and `value_row_count=len(patches)` to the shared aligner; delete the spot-id-only dictionary; use validated `_patch_source_row` ordinals to index the patch array; and return reset metadata-order aligned rows. In the foundation cache-hit branch, read cached IDs without `astype(str)`, validate embedding/ID cardinality, construct a DataFrame containing exact requested `slide_id` plus cached `spot_id`, and call the same aligner before returning cached embeddings indexed by `_patch_source_row`. On the cache-miss path, preserve exact aligned string IDs when creating the current safe Unicode cache payload, verify extracted embedding cardinality before any write, and retain the existing cache filename/schema pending Phases 4/5. Add forbidden-side-effect tests proving malformed/cache-subset/duplicate/cross-slide/cardinality defects stop before array indexing, encoder/model work, and `np.savez_compressed`; add valid cache-hit/miss/CNN/RF-equivalent ordering tests. Do not change encoder selection, model training, fold policy, target scaling, RF imputation, or unsafe legacy patch deserialization in this phase.</action>
  <acceptance_criteria>
    <criterion>Ordinary patch loading indexes arrays only with validated `_patch_source_row` and never constructs a lossy spot_id dictionary.</criterion>
    <criterion>Foundation cache hits require exact complete compound-key equality and embedding/metadata cardinality; cached subsets and string coercion are rejected.</criterion>
    <criterion>Equivalent ordinary and foundation inputs produce the same compound-key order and paired labels, including independently shuffled labels.</criterion>
    <criterion>Invalid consumer inputs reach no NumPy indexing, encoder/model, or cache-write sentinel.</criterion>
    <criterion>Existing cache names/payload fields, public signatures, valid output shapes, and foundation tests remain compatible.</criterion>
    <criterion>The consumer-focused and affected regression commands exit 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_identity_alignment.py projects/spatial-pharma-dl/tests/test_foundation.py projects/spatial-pharma-dl/tests/test_empty_boundaries.py &amp;&amp; python -m ruff check projects/spatial-pharma-dl/src/identity.py projects/spatial-pharma-dl/src/train.py projects/spatial-pharma-dl/src/foundation.py projects/spatial-pharma-dl/tests/test_identity_alignment.py</automated>
  </verify>
  <done>Every ordinary and foundation consumer uses identical complete compound identity and array-order evidence before scientific or cache side effects.</done>
</task>

</tasks>

<verification>
1. Run `python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_identity_alignment.py`.
2. Run affected synthetic AnnData, foundation, empty-boundary, and model/fold smoke modules.
3. Run Ruff over every modified source and test file.
4. Search the touched production paths for remaining `astype(str)`, spot-id-only dictionaries/set inclusion, or unconstrained identity merges and confirm none remain in alignment code.
5. Confirm no cache-format migration, artifact durability, fold/class policy, leakage/scaling/imputation, image-science, label-confidence, CLI, notebook, output-name, or public-export change entered the diff.
</verification>

<success_criteria>
- Every label and patch/cache row is admitted by exact unique `(slide_id, spot_id)` identity with deterministic actionable failures.
- Shuffled complete data aligns one-to-one in patch/cache order without losing targets or provenance.
- Ordinary patch, CNN/RF, and foundation cache hit/miss paths share one strict alignment contract and reject cardinality mismatch before indexing or expensive work.
- Established signatures, shapes, cache names, CLI/notebook surfaces, and public imports remain compatible.
- No high-severity T-01 through T-03 control remains open.
</success_criteria>
