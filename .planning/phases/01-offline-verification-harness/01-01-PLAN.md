---
phase: 01-offline-verification-harness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - projects/spatial-pharma-dl/tests/conftest.py
  - projects/spatial-pharma-dl/tests/test_core_refactors.py
  - projects/spatial-pharma-dl/tests/test_foundation.py
  - projects/spatial-pharma-dl/tests/test_fixture_contracts.py
  - projects/spatial-pharma-dl/tests/test_verification_contract.py
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "D-01: The default offline evidence contract rejects network access, model-hub access, and tests without exactly one primary tier."
    - "D-02: Shared valid and adversarial fixture factories are deterministic, fresh per call, CPU-small, and confined to pytest temporary paths."
    - "D-03: Bare pytest selects only offline evidence; notebook_smoke, network, and full_cohort remain explicit mutually exclusive opt-ins selected by the canonical runner."
    - "D-04: Later phases can extend one shared conftest marker and fixture vocabulary without changing notebook, CLI, or public-import behavior."
  artifacts:
    - path: pyproject.toml
      provides: Strict pytest marker declarations and checked-in Ruff scope
    - path: projects/spatial-pharma-dl/tests/conftest.py
      provides: Primary-tier enforcement, offline socket denial, deterministic factories, and shared path setup
    - path: projects/spatial-pharma-dl/tests/test_verification_contract.py
      provides: Executable proof of tier and network-denial behavior
    - path: projects/spatial-pharma-dl/tests/test_fixture_contracts.py
      provides: Executable proof of fixture determinism, freshness, and adversarial coverage
  key_links:
    - from: pyproject.toml
      to: projects/spatial-pharma-dl/tests/conftest.py
      via: registered primary marker names consumed by collection enforcement
    - from: projects/spatial-pharma-dl/tests/conftest.py
      to: projects/spatial-pharma-dl/tests/test_verification_contract.py
      via: offline environment and socket guard exercised as public harness behavior
---

<objective>
As a maintainer, I want a strict offline collection contract and reusable deterministic fixtures so that every reliability regression is classified, repeatable, and unable to reach external services by accident.

Purpose: Establish the Wave 0 harness required by every later Phase 1 evidence module and by later reliability phases.
Output: Registered evidence tiers, checked-in lint scope, an enforced offline session, deterministic fixture factories, and focused contract tests.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-offline-verification-harness/01-CONTEXT.md
@.planning/phases/01-offline-verification-harness/01-RESEARCH.md
@.planning/phases/01-offline-verification-harness/01-VALIDATION.md
@.planning/phases/01-offline-verification-harness/01-PATTERNS.md
</execution_context>

<threat_model>
  <threat id="T-01" severity="high" blocking="true">An offline test silently opens a socket. Deny socket.socket.connect and socket.create_connection during offline sessions and raise an actionable error naming the offline tier.</threat>
  <threat id="T-02" severity="high" blocking="true">A model library downloads weights or remote code. Set HF_HUB_OFFLINE=1 and TRANSFORMERS_OFFLINE=1 in offline sessions and retain the socket denial.</threat>
  <threat id="T-03" severity="medium" blocking="false">Shared mutable or unseeded fixtures make results flaky. Use fresh factories, local fixed-seed generators, and repeated-construction assertions.</threat>
  <threat id="T-04" severity="high" blocking="true">Artifact fixtures normalize object deserialization. Generate object-valued inputs only as adversaries and never read them with allow_pickle=True.</threat>
  <security_gate>Do not complete this plan while any T-01, T-02, or T-04 control is absent or bypassable from the offline tier.</security_gate>
</threat_model>

<tasks>

<task type="auto" id="1-01-01">
  <name>Implement strict primary tiers and the offline execution boundary</name>
  <files>pyproject.toml, projects/spatial-pharma-dl/tests/conftest.py, projects/spatial-pharma-dl/tests/test_core_refactors.py, projects/spatial-pharma-dl/tests/test_foundation.py, projects/spatial-pharma-dl/tests/test_verification_contract.py</files>
  <read_first>
    <file>pyproject.toml</file>
    <file>projects/spatial-pharma-dl/tests/test_core_refactors.py</file>
    <file>projects/spatial-pharma-dl/tests/test_foundation.py</file>
    <file>.planning/phases/01-offline-verification-harness/01-CONTEXT.md</file>
    <file>.planning/phases/01-offline-verification-harness/01-VALIDATION.md</file>
    <file>.planning/phases/01-offline-verification-harness/01-PATTERNS.md</file>
  </read_first>
  <action>Add `[tool.pytest.ini_options]` marker declarations for exactly `offline`, `notebook_smoke`, `network`, and `full_cohort`, and set `addopts = "--strict-markers -m offline"` so bare `python -m pytest -q` selects only offline evidence. Preserve explicit override semantics: later `scripts/verify.py notebook-smoke|network|full-cohort` commands must pass their own `-m` value on the command line, which overrides the configured default. Add checked-in Ruff configuration covering Python 3.11 and the repository paths named in the research without changing setuptools package discovery. Create `conftest.py` that centralizes the existing `PHARMA` and repository-root import paths, validates during collection that each test has exactly one of the four primary markers, sets `HF_HUB_OFFLINE=1` and `TRANSFORMERS_OFFLINE=1` for offline selection, and denies both `socket.socket.connect` and `socket.create_connection` with an actionable offline-tier exception. External access may be enabled only for a selected `network` or `full_cohort` primary tier; do not add a per-test bypass. Add only a module-level `pytestmark = pytest.mark.offline` classification to the two existing test modules and preserve all eight test bodies. Create contract tests that prove zero/multiple primary markers fail classification, one primary marker passes, offline flags are set, attempted socket creation is denied without making a real external connection, and a subprocess collection probe for bare pytest deselects tests marked `notebook_smoke`, `network`, and `full_cohort` while collecting an `offline` sentinel.</action>
  <acceptance_criteria>
    <criterion>`pyproject.toml` declares all four primary markers, contains `addopts = "--strict-markers -m offline"`, and adds Ruff configuration without changing `[tool.setuptools.packages.find]` or `[tool.setuptools.package-dir]`.</criterion>
    <criterion>Every test collected from the two pre-existing modules has exactly the `offline` primary marker and all eight original test functions remain present.</criterion>
    <criterion>An offline test calling the guarded socket APIs raises an error that contains `offline` and does not reach a network endpoint.</criterion>
    <criterion>Tests with zero or two primary markers are rejected, while a test with exactly one primary marker is accepted.</criterion>
    <criterion>A contract test named `test_bare_pytest_defaults_to_offline` proves bare pytest collects the offline sentinel and deselects notebook-smoke, network, and full-cohort sentinels; no external-tier test body executes.</criterion>
    <criterion>The focused validation command exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_verification_contract.py projects/spatial-pharma-dl/tests/test_core_refactors.py projects/spatial-pharma-dl/tests/test_foundation.py</automated>
  </verify>
  <done>The existing regressions and the harness contract run as classified offline evidence, and the offline session fails closed on accidental network access.</done>
</task>

<task type="auto" id="1-01-02">
  <name>Add the deterministic valid and adversarial fixture vocabulary</name>
  <files>projects/spatial-pharma-dl/tests/conftest.py, projects/spatial-pharma-dl/tests/test_fixture_contracts.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/tests/conftest.py</file>
    <file>projects/spatial-pharma-dl/tests/test_foundation.py</file>
    <file>projects/spatial-pharma-dl/src/patches.py</file>
    <file>projects/spatial-pharma-dl/src/train.py</file>
    <file>utils/st_helpers.py</file>
    <file>.planning/phases/01-offline-verification-harness/01-RESEARCH.md</file>
    <file>.planning/phases/01-offline-verification-harness/01-PATTERNS.md</file>
  </read_first>
  <action>Extend `conftest.py` with factory fixtures, not mutable singleton fixtures, for: a real 8-16 spot AnnData with integer counts, unique spot/gene names, `slide_id`, full-resolution `obsm["spatial"]`, and Visium-style `uns["spatial"]` image/scalefactors; a stable three-slide cohort with viable class support and one regression target; null, duplicate, unmatched, and cross-slide key variants; empty, one-slide, single-class, and unseen-held-out-class fold variants; grayscale, wrong-channel, invalid-range, all-white, rank-deficient, and border image variants; and missing-key, wrong-shape/dtype, object-NPZ, corrupt-JSON, row-mismatch, and corrupt-byte artifact variants. Use local `np.random.default_rng` instances with named fixed seeds, return a fresh object on every call, and route all serialized paths through `tmp_path`. Add tests that compare repeated factory outputs, prove mutations do not leak, enumerate every required adversarial variant, and assert no factory writes under repository `data/` or `outputs/`. Do not assert later production fixes.</action>
  <acceptance_criteria>
    <criterion>Repeated construction with the same fixture seed yields equal arrays, keys, cohort ordering, and fold ordering.</criterion>
    <criterion>Mutating one returned AnnData, DataFrame, image, or artifact descriptor does not alter a later factory result.</criterion>
    <criterion>The fixture contract names and constructs every key, cohort/fold, image, and artifact adversary listed in the action.</criterion>
    <criterion>All filesystem fixture paths are descendants of pytest `tmp_path`; no test creates files under repository `data/` or `outputs/`.</criterion>
    <criterion>The focused validation command exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_fixture_contracts.py</automated>
  </verify>
  <done>Maintainers have one deterministic, fresh, CPU-small fixture vocabulary that later Phase 1 plans and subsequent reliability phases can extend.</done>
</task>

</tasks>

<verification>
1. Run `python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_core_refactors.py projects/spatial-pharma-dl/tests/test_foundation.py projects/spatial-pharma-dl/tests/test_fixture_contracts.py projects/spatial-pharma-dl/tests/test_verification_contract.py`.
2. Run bare `python -m pytest -q` and confirm the configured default selects only the offline primary tier.
3. Run `python -m ruff check utils scripts projects/spatial-pharma-dl/src projects/spatial-pharma-dl/scripts projects/spatial-pharma-dl/tests`.
4. Confirm the focused suite uses only CPU, temporary directories, and the enforced offline environment.
</verification>

<success_criteria>
- Strict collection distinguishes all four primary tiers and rejects zero/multiple classifications.
- Bare pytest defaults to offline evidence, while explicit runner selectors can opt into exactly one external tier.
- The offline tier denies sockets and sets both model-hub offline flags.
- All eight existing regressions remain green and are classified offline.
- Deterministic valid/adversarial fixtures are fresh, repeatable, and reusable by later plans.
- No high-severity threat in T-01, T-02, or T-04 remains open.
</success_criteria>
