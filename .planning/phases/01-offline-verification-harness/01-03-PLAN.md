---
phase: 01-offline-verification-harness
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - scripts/verify.py
  - projects/spatial-pharma-dl/tests/test_verification_contract.py
  - .github/workflows/verify.yml
  - README.md
  - projects/spatial-pharma-dl/README.md
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "D-01: `python scripts/verify.py fast` runs checked-in Ruff scope followed by the complete offline pytest tier and propagates failures."
    - "D-02: The canonical command consumes the deterministic CPU-small evidence from Plans 01 and 02 and completes within the 300-second target."
    - "D-03: Maintainers and CI can distinguish fast, notebook-smoke, network, and full-cohort commands; only fast is required on pushes and pull requests."
    - "D-04: Documentation tells later phases to extend these same markers, fixtures, runner commands, and CI entry points while preserving public notebook and pipeline interfaces."
  artifacts:
    - path: scripts/verify.py
      provides: Canonical cross-platform tier runner
      exports: [build_commands, main]
    - path: .github/workflows/verify.yml
      provides: Required offline fast job and explicit opt-in tier entry points
    - path: README.md
      provides: Repository-level verification command and tier documentation
    - path: projects/spatial-pharma-dl/README.md
      provides: Pharma-focused verification and direct debugging commands
  key_links:
    - from: .github/workflows/verify.yml
      to: scripts/verify.py
      via: exact `python scripts/verify.py fast` required-job command
    - from: scripts/verify.py
      to: pyproject.toml
      via: Ruff scope and pytest marker names registered in Wave 1
    - from: README.md
      to: scripts/verify.py
      via: documented canonical tier commands
---

<objective>
As a maintainer, I want one documented verification command used identically by local development and CI so that fast reliability evidence is easy to run, failures are visible, and external tiers are never mistaken for required offline evidence.

Purpose: Turn the strict harness and representative tests into the canonical TEST-01 maintainer workflow.
Output: A testable Python runner, required/opt-in CI jobs, and concise repository/pharma documentation.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-offline-verification-harness/01-CONTEXT.md
@.planning/phases/01-offline-verification-harness/01-RESEARCH.md
@.planning/phases/01-offline-verification-harness/01-VALIDATION.md
@.planning/phases/01-offline-verification-harness/01-PATTERNS.md
@.planning/phases/01-offline-verification-harness/01-01-PLAN.md
@.planning/phases/01-offline-verification-harness/01-02-PLAN.md
</execution_context>

<threat_model>
  <threat id="T-01" severity="high" blocking="true">The canonical or CI fast path may omit the socket guard or select external tests. The runner must select exactly `-m offline`, and CI must set offline flags and invoke only the canonical fast command.</threat>
  <threat id="T-02" severity="high" blocking="true">CI may populate or download model weights. Install dependencies only, do not cache weights/scientific artifacts, and run the guarded offline command.</threat>
  <threat id="T-03" severity="medium" blocking="false">Local and CI commands may diverge and hide fixture flakiness. CI invokes the identical public command, and runner construction is unit-tested as argument lists.</threat>
  <threat id="T-04" severity="high" blocking="true">CI success may imply unsafe production artifact readers were tested. Documentation and job names must state that Phase 1 covers safe fixture round trips, not the Phase 5 production migration.</threat>
  <security_gate>Do not complete this plan if the required CI job can select network/full-cohort tests, download weights, cache scientific artifacts, or bypass the fast runner.</security_gate>
</threat_model>

<tasks>

<task type="auto" id="1-03-01">
  <name>Add and test the canonical tier runner</name>
  <files>scripts/verify.py, projects/spatial-pharma-dl/tests/test_verification_contract.py</files>
  <read_first>
    <file>pyproject.toml</file>
    <file>projects/spatial-pharma-dl/tests/conftest.py</file>
    <file>projects/spatial-pharma-dl/tests/test_verification_contract.py</file>
    <file>scripts/generate_gallery_figures.py</file>
    <file>projects/spatial-pharma-dl/scripts/run_pipeline.py</file>
    <file>.planning/phases/01-offline-verification-harness/01-VALIDATION.md</file>
    <file>.planning/phases/01-offline-verification-harness/01-PATTERNS.md</file>
  </read_first>
  <action>Create import-safe `scripts/verify.py` with `fast`, `notebook-smoke`, `network`, and `full-cohort` choices. Expose deterministic command construction (for example `build_commands(tier)`) and a `main` entry point. `fast` must construct two argument-list subprocesses in order: Ruff over `utils`, root `scripts`, pharma `src`, pharma `scripts`, and pharma `tests`; then `sys.executable -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests`. The three opt-in commands must pass their own explicit command-line `-m notebook_smoke`, `-m network`, or `-m full_cohort` selector so they override the `pyproject.toml` default-offline `addopts`; test this override behavior rather than relying on implicit pytest configuration. Use `subprocess.run(..., check=True)`, print the selected tier and exact commands, stop on the first nonzero result, and return a distinct documented message/status when an opt-in tier has zero tests rather than presenting emptiness as passed evidence. Do not import Scanpy, Squidpy, Torch, torchvision, timm, Transformers, datasets, or production pharma modules at module import time. Extend `test_verification_contract.py` to unit-test command arrays/order, invalid tiers, failure propagation, explicit opt-in override selections, and empty opt-in reporting without recursively launching the full suite.</action>
  <acceptance_criteria>
    <criterion>`python scripts/verify.py --help` lists exactly `fast`, `notebook-smoke`, `network`, and `full-cohort` tier choices.</criterion>
    <criterion>The fast command builder returns Ruff first and pytest second; the pytest command contains `--strict-markers`, `-m`, and `offline`.</criterion>
    <criterion>Each opt-in command contains an explicit command-line `-m` selector for only its corresponding primary marker, overrides default-offline `addopts`, and reports zero selected tests as `no tests defined for this opt-in tier` or an equivalently explicit non-evidence status.</criterion>
    <criterion>A simulated Ruff or pytest failure stops later subprocesses and propagates a nonzero exit code.</criterion>
    <criterion>`scripts/verify.py` imports no scientific/model/download modules at module import time, and the focused contract tests exit 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_verification_contract.py &amp;&amp; python scripts/verify.py fast</automated>
  </verify>
  <done>Maintainers have one testable cross-platform command whose fast mode runs Ruff and all offline evidence in a clean process.</done>
</task>

<task type="auto" id="1-03-02">
  <name>Use the canonical runner in documentation and CI</name>
  <files>.github/workflows/verify.yml, README.md, projects/spatial-pharma-dl/README.md, projects/spatial-pharma-dl/tests/test_verification_contract.py</files>
  <read_first>
    <file>scripts/verify.py</file>
    <file>pyproject.toml</file>
    <file>requirements.txt</file>
    <file>projects/spatial-pharma-dl/requirements-pharma.txt</file>
    <file>README.md</file>
    <file>projects/spatial-pharma-dl/README.md</file>
    <file>projects/spatial-pharma-dl/tests/test_verification_contract.py</file>
    <file>.planning/PROJECT.md</file>
    <file>.planning/phases/01-offline-verification-harness/01-CONTEXT.md</file>
    <file>.planning/phases/01-offline-verification-harness/01-RESEARCH.md</file>
  </read_first>
  <action>Create `.github/workflows/verify.yml` triggered on pull requests and pushes to `main`, plus `workflow_dispatch` inputs for explicit opt-in tiers. Define a required Ubuntu/Python 3.11 CPU `fast` job that installs the existing base and pharma requirement files plus `pytest`, `ruff`, and `nbformat`, sets `HF_HUB_OFFLINE=1` and `TRANSFORMERS_OFFLINE=1` at job scope, and runs exactly `python scripts/verify.py fast`. Define separately named and dispatch-gated `notebook-smoke`, `network`, and `full-cohort` jobs that call their matching runner tier; none may be a dependency of the required fast job. Cache dependencies only; do not cache repository `data/`, `outputs/`, model weights, or scientific artifacts. Add a concise `Verification` section to the root README near Setup and to the pharma README near Quickstart listing all four canonical commands, the direct Ruff/strict-marker pytest debugging commands, the CPU/offline/default versus opt-in boundaries, the safe-fixture-versus-production-migration distinction, and the rule that later phases extend the same marker/fixture/runner conventions. Extend `test_verification_contract.py` with `test_verification_workflow_contract`, parsing `.github/workflows/verify.yml` with `yaml.BaseLoader` (so the `on` key is preserved) and asserting pull-request/push-to-main triggers, workflow_dispatch opt-in inputs, Ubuntu and Python 3.11, both offline flags, the exact fast and opt-in runner commands, no fast-job dependency on opt-in jobs, explicit opt-in job gating, and absence of cache paths containing `data`, `outputs`, weight/model-cache, or scientific-artifact directories. Add `test_verification_documentation_contract`, reading both README files and asserting each contains all four canonical commands, the direct Ruff and offline pytest debug commands, wording that fast is default CPU/offline and the other tiers are opt-in, wording that safe fixture round trips do not certify production cache/checkpoint migration, and wording that later phases extend the same marker/fixture/runner convention. Do not change notebook ordering, pipeline commands, config keys, output names, package discovery, or public imports.</action>
  <acceptance_criteria>
    <criterion>The workflow's required fast job triggers for pull requests and pushes to `main`, uses Python 3.11 on Ubuntu, and contains exactly `python scripts/verify.py fast` as its verification invocation.</criterion>
    <criterion>The fast job sets both model-hub offline flags and has no dependency on notebook-smoke, network, or full-cohort jobs.</criterion>
    <criterion>Opt-in jobs are separately named, explicitly gated, and call only their matching runner tiers.</criterion>
    <criterion>The workflow does not cache `data/`, `outputs/`, pretrained weights, or generated scientific artifacts.</criterion>
    <criterion>Both README files document four canonical tiers, direct debug commands, required-versus-opt-in semantics, and the shared extension convention.</criterion>
    <criterion>`test_verification_workflow_contract` parses the workflow and asserts triggers, Python 3.11, both offline flags, exact runner commands, opt-in gating/job independence, and prohibited cache paths.</criterion>
    <criterion>`test_verification_documentation_contract` asserts both README files contain all four commands, direct debug commands, the safe-fixture evidence boundary, and the later-phase extension convention.</criterion>
    <criterion>The phase acceptance command exits 0 in under 300 seconds on CPU.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_verification_contract.py -k "verification_workflow_contract or verification_documentation_contract" &amp;&amp; python scripts/verify.py fast</automated>
  </verify>
  <done>Local documentation and CI use the same canonical fast command, while every external evidence tier is visible and opt-in.</done>
</task>

</tasks>

<verification>
1. Run `python scripts/verify.py fast` in a clean process and require exit 0 within 300 seconds.
2. Run bare `python -m pytest -q` and confirm no notebook-smoke, network, or full-cohort test executes.
3. Confirm fast output runs Ruff first and reports nonzero offline unit, artifact, AnnData integration, model/fold, and notebook-structure evidence.
4. Run the workflow and documentation contract tests to parse `.github/workflows/verify.yml` and inspect both README files.
5. Run `python scripts/verify.py --help` and verify all four documented commands are present.
</verification>

<success_criteria>
- One canonical fast command runs Ruff and the complete strict offline tier with failure propagation.
- Required CI is CPU/Python 3.11/offline and invokes the exact canonical command.
- Notebook-smoke, network, and full-cohort tiers remain explicit, separate opt-ins.
- Documentation makes the evidence boundary and later-phase extension convention unambiguous.
- No high-severity threat in T-01, T-02, or T-04 remains open.
</success_criteria>
