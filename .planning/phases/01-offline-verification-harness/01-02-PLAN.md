---
phase: 01-offline-verification-harness
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - projects/spatial-pharma-dl/tests/test_artifact_roundtrips.py
  - projects/spatial-pharma-dl/tests/test_synthetic_anndata.py
  - projects/spatial-pharma-dl/tests/test_model_fold_smoke.py
  - projects/spatial-pharma-dl/tests/test_notebook_structure.py
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "D-01: The fast offline evidence contains nonzero artifact, AnnData integration, model/fold, and notebook-structure checks without downloads or private data."
    - "D-02: Representative evidence consumes the deterministic shared fixture factories and stays bounded to small CPU workloads."
    - "D-03: Structural notebook validation remains offline while executable notebooks, external integration, and full-cohort work remain separate opt-in tiers."
    - "D-04: Tests exercise current public seams without changing notebook numbering, documented CLI behavior, public imports, or implementing later production fixes."
  artifacts:
    - path: projects/spatial-pharma-dl/tests/test_artifact_roundtrips.py
      provides: Safe primitive NPZ, Parquet, JSON, and H5AD fixture-format evidence
    - path: projects/spatial-pharma-dl/tests/test_synthetic_anndata.py
      provides: Real tiny AnnData spatial and patch integration evidence
    - path: projects/spatial-pharma-dl/tests/test_model_fold_smoke.py
      provides: Bounded CPU model and deterministic LOSO orchestration evidence
    - path: projects/spatial-pharma-dl/tests/test_notebook_structure.py
      provides: Structural validation for all 20 numbered notebooks
  key_links:
    - from: projects/spatial-pharma-dl/tests/conftest.py
      to: projects/spatial-pharma-dl/tests/test_artifact_roundtrips.py
      via: shared artifact and tmp_path factories
    - from: projects/spatial-pharma-dl/tests/test_synthetic_anndata.py
      to: utils/st_helpers.py
      via: public image and scalefactor accessors
    - from: projects/spatial-pharma-dl/tests/test_model_fold_smoke.py
      to: projects/spatial-pharma-dl/src/train.py
      via: loso_folds and monkeypatched train_loso orchestration seams
---

<objective>
As a maintainer, I want representative offline evidence for artifacts, spatial data, model/fold behavior, and notebooks so that the fast tier detects meaningful tutorial and pharma regressions without claiming later scientific migrations are already complete.

Purpose: Fill the strict Wave 1 harness with the representative TEST-01 evidence categories.
Output: Four focused offline test modules using shared deterministic fixtures and current public seams.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-offline-verification-harness/01-CONTEXT.md
@.planning/phases/01-offline-verification-harness/01-RESEARCH.md
@.planning/phases/01-offline-verification-harness/01-VALIDATION.md
@.planning/phases/01-offline-verification-harness/01-PATTERNS.md
@.planning/phases/01-offline-verification-harness/01-01-PLAN.md
</execution_context>

<threat_model>
  <threat id="T-01" severity="high" blocking="true">Representative tests may trigger hidden HTTP access. Every module is `offline` and runs under the Wave 1 socket guard.</threat>
  <threat id="T-02" severity="high" blocking="true">Model smoke may download pretrained weights or remote code. Call `build_model(..., pretrained=False)`, avoid timm/Transformers backends, and fail if a weight loader is reached.</threat>
  <threat id="T-03" severity="medium" blocking="false">Scientific and model fixtures may become flaky through global RNG or shared state. Consume fresh fixed-seed factories and assert stable fold ordering rather than platform-specific bit patterns.</threat>
  <threat id="T-04" severity="high" blocking="true">Artifact evidence may bless the unsafe production pickle paths. Load numeric/string NPZ with `allow_pickle=False`, prove object payload access is rejected, and do not call unsafe patch/checkpoint readers.</threat>
  <security_gate>Do not complete this plan if a representative test requires network, pretrained weights, repository caches, allow_pickle=True, or weights_only=False.</security_gate>
</threat_model>

<tasks>

<task type="auto" id="1-02-01">
  <name>Prove safe primitive artifact fixture round trips</name>
  <files>projects/spatial-pharma-dl/tests/test_artifact_roundtrips.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/tests/conftest.py</file>
    <file>projects/spatial-pharma-dl/src/foundation.py</file>
    <file>projects/spatial-pharma-dl/src/patches.py</file>
    <file>utils/st_helpers.py</file>
    <file>.planning/phases/01-offline-verification-harness/01-RESEARCH.md</file>
    <file>.planning/phases/01-offline-verification-harness/01-PATTERNS.md</file>
  </read_first>
  <action>Create an `offline` artifact test module that round-trips float32/numeric arrays and Unicode spot IDs through compressed NPZ and reads with explicit `allow_pickle=False`; tabular metadata through Parquet with `index=False`; primitive-only metadata through JSON; and the shared real AnnData fixture through H5AD. Assert required keys, dtypes, shapes, row order, spot IDs, and spatial metadata after reading. Create an object-valued NPZ adversary and assert that access under `allow_pickle=False` raises. Do not call `load_patch_arrays()` or `load_model_from_checkpoint()`, and do not include any `allow_pickle=True` or `weights_only=False` call in this module.</action>
  <acceptance_criteria>
    <criterion>NPZ, Parquet, JSON, and H5AD round trips preserve their declared keys, dtypes, shapes, row ordering, IDs, and spatial metadata.</criterion>
    <criterion>The NPZ reader call contains `allow_pickle=False`, and object-valued payload access raises instead of deserializing.</criterion>
    <criterion>The module contains no `allow_pickle=True`, `weights_only=False`, `load_patch_arrays`, or `load_model_from_checkpoint` usage.</criterion>
    <criterion>Every artifact path is under `tmp_path`, and the focused validation command exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_artifact_roundtrips.py</automated>
  </verify>
  <done>Primitive fixture formats have executable safe round-trip evidence without normalizing current unsafe production readers.</done>
</task>

<task type="auto" id="1-02-02">
  <name>Exercise a real synthetic AnnData spatial path</name>
  <files>projects/spatial-pharma-dl/tests/test_synthetic_anndata.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/tests/conftest.py</file>
    <file>utils/st_helpers.py</file>
    <file>projects/spatial-pharma-dl/src/patches.py</file>
    <file>.planning/phases/01-offline-verification-harness/01-PATTERNS.md</file>
  </read_first>
  <action>Create an `offline` integration module using the shared real AnnData factory. Verify H5AD preserves axes and spatial metadata; `st.get_image` returns the deterministic RGB hires image; `st.get_scalefactors` returns `tissue_hires_scalef` and `spot_diameter_fullres`; `coords_hires` applies the declared scale; and `extract_all_patches_for_slide` returns one patch and metadata row per valid spot with stable spot IDs and compatible tensor/table shapes. Include center and border coordinates, but assert only current shape/alignment behavior rather than the Phase 8 padding contract. Perform a simple one-to-one valid label/patch merge without asserting later invalid-key rejection behavior.</action>
  <acceptance_criteria>
    <criterion>The round-tripped AnnData retains ordered obs/var names, `obsm["spatial"]`, and Visium-style `uns["spatial"]` content.</criterion>
    <criterion>Image/scalefactor access and coordinate scaling return the fixture's declared values.</criterion>
    <criterion>Patch count equals metadata row count and valid spot count; spot ID order remains stable.</criterion>
    <criterion>The focused validation command exits 0 without downloading data or writing to repository caches.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_synthetic_anndata.py</automated>
  </verify>
  <done>A tiny real spatial object crosses the existing image, scale, coordinate, patch, and valid-alignment seams entirely offline.</done>
</task>

<task type="auto" id="1-02-03">
  <name>Add a bounded CPU model and LOSO orchestration smoke</name>
  <files>projects/spatial-pharma-dl/tests/test_model_fold_smoke.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/tests/conftest.py</file>
    <file>projects/spatial-pharma-dl/tests/test_core_refactors.py</file>
    <file>projects/spatial-pharma-dl/tests/test_foundation.py</file>
    <file>projects/spatial-pharma-dl/src/models.py</file>
    <file>projects/spatial-pharma-dl/src/train.py</file>
    <file>.planning/phases/01-offline-verification-harness/01-RESEARCH.md</file>
  </read_first>
  <action>Create an `offline` smoke module with one forward/backward/optimizer step on a fresh tiny local multi-head model and small patch batch. Add one public `build_model` output-shape test that passes `model_name="resnet18"` and `pretrained=False`; guard or monkeypatch weight-loading so reaching a pretrained loader fails clearly. Verify `loso_folds` yields stable order and each of three slides is held out exactly once. Exercise `train_loso` only with `train_one_fold` monkeypatched to a no-write deterministic stub, and assert each held-out slide appears once. Do not run real `train_one_fold`, foundation encoders, GPU code, checkpoint loading, or outer-slide early-stopping behavior.</action>
  <acceptance_criteria>
    <criterion>The tiny model completes exactly one CPU optimization step with finite loss and expected classification/regression output shapes.</criterion>
    <criterion>The public ResNet18 smoke passes `pretrained=False` and fails if a pretrained weight loader is invoked.</criterion>
    <criterion>LOSO enumeration and stubbed orchestration hold out every input slide exactly once in deterministic order.</criterion>
    <criterion>No repository output/checkpoint path is created, and the focused validation command exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_model_fold_smoke.py</automated>
  </verify>
  <done>The fast tier detects model tensor and fold-orchestration regressions without weight downloads, expensive training, or scientific leakage claims.</done>
</task>

<task type="auto" id="1-02-04">
  <name>Validate all committed notebook structures offline</name>
  <files>projects/spatial-pharma-dl/tests/test_notebook_structure.py</files>
  <read_first>
    <file>README.md</file>
    <file>projects/spatial-pharma-dl/README.md</file>
    <file>scripts/patch_notebooks.py</file>
    <file>projects/spatial-pharma-dl/scripts/build_notebooks.py</file>
    <file>projects/spatial-pharma-dl/scripts/build_foundation_notebook.py</file>
    <file>.planning/phases/01-offline-verification-harness/01-PATTERNS.md</file>
  </read_first>
  <action>Create an `offline` structural test that discovers repository-relative notebooks and asserts the established numeric sequences: 13 root notebooks `00` through `12`, and seven pharma notebooks `01` through `07`. Parse each with `nbformat.read`, call `nbformat.validate`, assert it is nonempty, allow only markdown/code/raw cells, require code-cell source to be text, and verify root notebooks retain the established Python kernel family while pharma notebooks retain the `spatial-tx` kernel family. Do not execute notebooks, rewrite metadata, require cleared outputs, enforce exact cell counts, or require cell IDs.</action>
  <acceptance_criteria>
    <criterion>Discovery reports exactly 13 ordered root notebooks and seven ordered pharma notebooks with no duplicate or missing numeric prefix.</criterion>
    <criterion>All 20 notebooks parse and pass `nbformat.validate`; all cells use allowed types and code sources are text.</criterion>
    <criterion>Kernel assertions distinguish the established root and pharma kernel families.</criterion>
    <criterion>No notebook is modified, and the focused validation command exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_notebook_structure.py</automated>
  </verify>
  <done>Every committed teaching notebook has fast structural coverage without execution, downloads, or compatibility-breaking rewrites.</done>
</task>

</tasks>

<verification>
1. Run each focused command from `01-VALIDATION.md` for tasks 1-02-01 through 1-02-04.
2. Run `python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests` and confirm all four new evidence categories collect nonzero tests.
3. Confirm the tests do not call unsafe production readers, real training, network/model hubs, repository data/output helpers, or notebook execution.
</verification>

<success_criteria>
- Safe primitive artifact formats round-trip under `allow_pickle=False`, and object payloads are rejected.
- A real tiny AnnData traverses current spatial/image/patch seams with stable alignment.
- A bounded CPU model step and deterministic LOSO orchestration complete without downloads or writes.
- All 20 notebooks pass structural checks while executable notebook work remains outside the offline tier.
- No high-severity threat in T-01, T-02, or T-04 remains open.
</success_criteria>

