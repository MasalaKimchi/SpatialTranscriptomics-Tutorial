---
phase: 04-durable-artifact-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/artifacts.py
  - projects/spatial-pharma-dl/tests/conftest.py
  - projects/spatial-pharma-dl/tests/test_artifact_contract.py
autonomous: true
requirements:
  - ART-03
  - ART-04
must_haves:
  truths:
    - "D-01/D-03: No payload decoder callback runs until a bounded duplicate-key-rejecting exact-primitive sidecar is complete, supported, fingerprint-matched, regular-file admitted, byte-count matched, checksum matched, and stable across hashing."
    - "D-02: Every declared artifact kind has an explicit contract version and allowlisted canonical fingerprint projection; relevant config/source/upstream/code-contract changes alter the digest, while paths, timestamps, presentation settings, insertion order, and unrelated configuration do not."
    - "D-04: Publication uses unique same-directory temporary files, validates the temporary pair through the same reader callback, fsyncs files/directories, replaces payload first and completed manifest last, and leaves no falsely reusable mixed generation at any injected failure point."
    - "D-05: The generic layer imports only the standard library, exposes immutable canonical records with fresh views and structured bounded errors, and makes no claim that checksums authenticate hostile pickle-bearing payloads."
  artifacts:
    - path: utils/artifacts.py
      provides: Import-light canonical fingerprints/manifests, strict admission, streaming integrity checks, reusable status, and atomic publication
      exports: [ArtifactFingerprint, ArtifactManifest, ArtifactAdmission, ArtifactValidationError, build_fingerprint, manifest_path, admit_artifact, artifact_reuse_status, publish_artifact]
    - path: projects/spatial-pharma-dl/tests/test_artifact_contract.py
      provides: Pure hostile admission, positive/negative fingerprint invariance, checksum/race rejection, and complete new/replacement fault-injection evidence
    - path: projects/spatial-pharma-dl/tests/conftest.py
      provides: Fresh deterministic artifact generations, malformed sidecars, and operation-log/fault fixtures under tmp_path
  key_links:
    - from: utils/artifacts.py
      to: every Phase 4 payload adapter
      via: one admit_artifact/publish_artifact contract and explicit production-reader callback
    - from: projects/spatial-pharma-dl/tests/test_artifact_contract.py
      to: utils/artifacts.py
      via: callbacks prove admission-before-decode and temporary-validation-before-replace ordering
---

<objective>
As a maintainer, I want one small artifact contract beneath every scientific format so that cache identity, completion, integrity, and publication ordering cannot diverge by artifact kind.

Purpose: Establish the trusted Phase 4 primitive before any H5AD, NPZ, table, checkpoint, report, or manifest integration depends on it.
Output: An import-light shared module, reusable deterministic fixtures, and exhaustive pure/fault-injection evidence.
</objective>

<execution_context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-durable-artifact-contract/04-CONTEXT.md
@.planning/phases/04-durable-artifact-contract/04-RESEARCH.md
@.planning/phases/04-durable-artifact-contract/04-VALIDATION.md
@.planning/phases/04-durable-artifact-contract/04-PATTERNS.md
@.planning/phases/02-validated-run-and-cohort-admission/02-04-SUMMARY.md
@.planning/phases/03-identity-and-adaptive-preprocessing/03-VERIFICATION.md
</execution_context>

<threat_model>
  <threat id="T-01" severity="high" blocking="true">Malformed, oversized, duplicate-key, deeply nested, or hostile manifest values execute hooks, exhaust resources, or inject diagnostics before rejection.</threat>
  <threat id="T-02" severity="high" blocking="true">A generic full-config hash either misses relevant lineage or invalidates caches for presentation-only changes.</threat>
  <threat id="T-03" severity="high" blocking="true">Existence checks, symlinks, truncation, replacement races, or byte/checksum drift reach the payload decoder.</threat>
  <threat id="T-04" severity="high" blocking="true">A two-file publication interruption exposes a new payload with an old/missing sidecar as valid.</threat>
  <threat id="T-05" severity="high" blocking="true">Writer validation uses a test-only parser or different path from production admission.</threat>
  <security_gate>Block Plan 04-02 until T-01 through T-05 are closed for the generic primitive, all fingerprint stability/invalidation assertions pass, and every publication fault state is either the old valid generation or rejected.</security_gate>
</threat_model>

<tasks>

<task type="auto" id="4-01-01">
  <name>Create Wave 0 contract fixtures and exact canonical fingerprint/manifest admission</name>
  <files>projects/spatial-pharma-dl/tests/conftest.py, projects/spatial-pharma-dl/tests/test_artifact_contract.py, utils/artifacts.py</files>
  <read_first>
    <file>projects/spatial-pharma-dl/src/validation.py</file>
    <file>projects/spatial-pharma-dl/src/identity.py</file>
    <file>projects/spatial-pharma-dl/tests/conftest.py</file>
    <file>projects/spatial-pharma-dl/tests/test_fixture_contracts.py</file>
    <file>projects/spatial-pharma-dl/tests/test_artifact_roundtrips.py</file>
    <file>.planning/phases/04-durable-artifact-contract/04-PATTERNS.md</file>
    <file>.planning/phases/04-durable-artifact-contract/04-VALIDATION.md</file>
  </read_first>
  <action>Write `test_artifact_contract.py` first with `pytestmark = pytest.mark.offline`; extend the existing artifact fixture vocabulary with fresh valid old/new payload generations, canonical sidecars, raw malformed bytes, exact fault-operation logs, and hostile parsed candidates without serializing hostile objects. Then add `utils.artifacts` using only the standard library. Define frozen slotted fingerprint/manifest/admission records whose internal state is canonical JSON and whose `to_dict()` methods return fresh exact-built-in trees. Define `ArtifactValidationError(ValueError)` with trusted `artifact_kind`, bounded validated basename, stable `reason_code`, and fixed regeneration guidance; never render raw JSON, parser exceptions, absolute paths, archive members, or caller object/type representations. Bound sidecar bytes before UTF-8/JSON, reject duplicate keys via `object_pairs_hook`, then exact-type-admit root/key/value primitives with explicit depth/node/map/list/string/integer budgets, finite floats, exact key sets, basename-only filename, supported schema/kind/contract/algorithm, `complete is True`, and lowercase 64-hex digests before canonicalization. Implement an explicit per-kind projection registry/constants for root H5AD stages, processed slide, label/domain tables, patch/index, embedding, checkpoint, reports/summaries, and cohort/preprocessing JSON wrappers; projections accept only already admitted exact primitives and allowlist contract/config/source/upstream fields. Add parameterized positive invariance tests for mapping order, output/checkout paths, timestamps, plotting/report formatting, and unrelated settings, plus negative tests for every kind's relevant config, source semantic/content identity, upstream lineage, target/model identity, and contract version.</action>
  <acceptance_criteria>
    <criterion>The new test module exists before implementation and covers the complete manifest adversarial matrix plus every artifact-kind projection.</criterion>
    <criterion>Equivalent admitted inputs produce byte-identical canonical input JSON and digests; caller mutation cannot alter stored state or prior views.</criterion>
    <criterion>Every relevant per-kind input change deterministically changes the digest, and every explicitly irrelevant presentation/path/order change leaves it identical.</criterion>
    <criterion>Oversized/deep/wide/duplicate/malformed/unsupported/hostile candidates fail with a structured bounded single-line error before payload or caller hooks.</criterion>
    <criterion>`utils.artifacts` imports without NumPy, pandas, torch, anndata, Scanpy, torchvision, timm, or transformers.</criterion>
    <criterion>The focused manifest/fingerprint command exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_artifact_contract.py -k "manifest or fingerprint or hostile or projection"</automated>
  </verify>
  <done>Canonical artifact identity and inspectable manifest state are deterministic, bounded, immutable, and explicit for every supported artifact kind.</done>
</task>

<task type="auto" id="4-01-02">
  <name>Admit regular stable payload bytes before the production reader callback</name>
  <files>utils/artifacts.py, projects/spatial-pharma-dl/tests/test_artifact_contract.py</files>
  <read_first>
    <file>utils/artifacts.py</file>
    <file>projects/spatial-pharma-dl/tests/test_artifact_contract.py</file>
    <file>.planning/phases/04-durable-artifact-contract/04-RESEARCH.md</file>
    <file>.planning/phases/04-durable-artifact-contract/04-PATTERNS.md</file>
  </read_first>
  <action>Implement pure `manifest_path()` and an `artifact_reuse_status()`/`admit_artifact()` path that creates no directories, deletes nothing, and never loads defaults. Require non-symlink regular manifest and payload files; parse/admit the sidecar; recompute its fingerprint digest; compare declared and expected digests with `hmac.compare_digest`; require expected kind/contract/logical final basename/completion; stream SHA-256 in bounded chunks from one opened descriptor; compare `fstat` device/inode/mode/size/mtime evidence before and after hashing; and verify declared byte count/checksum before invoking the supplied production-reader callback. Normal reuse derives the logical basename from the final path; only the publisher's private temporary-validation call may supply the trusted final basename while reading uniquely named temp paths, so the exact sidecar bytes validated before replacement remain valid after replacement and are never rewritten. Bind path-based readers to the admitted generation by comparing `lstat`/`stat` identity with the hashed descriptor immediately before and after the callback (descriptor-capable adapters should consume the admitted descriptor directly); a replace/truncate seam must become `unstable_payload`, never a decoded success. Wrap only expected parser/schema callback failures in stable reason codes without broad filename-only fallback. Add forbidden callbacks and tests for absent/legacy/zero-byte/invalid UTF-8/JSON/incomplete/stale pairs, missing/nonregular/symlink payloads, truncation/appending, declared count/checksum mismatch, payload replacement during hashing or around the reader callback, orphan temps, old-sidecar/new-payload, completed-sidecar/missing-payload, and callback schema failure with a valid checksum. Assert read/status calls leave missing parent paths absent.</action>
  <acceptance_criteria>
    <criterion>No decoder callback runs before manifest, expected fingerprint, regular-file state, stable byte count, and checksum all pass.</criterion>
    <criterion>Legacy, stale, incomplete, mixed-generation, replaced, truncated, appended, symlinked, and missing artifacts return/raise typed non-reusable states with regeneration guidance.</criterion>
    <criterion>A valid checksum with a callback-declared semantic schema failure remains rejected as `payload_schema_mismatch`/`reader_validation_failed`.</criterion>
    <criterion>Admission and status checks have no mkdir, cleanup, rewrite, default-config load, model, or scientific side effect.</criterion>
    <criterion>The focused integrity command and scoped Ruff command exit 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_artifact_contract.py -k "checksum or incomplete or stale or truncated or symlink or callback or side_effect" &amp;&amp; python -m ruff check utils/artifacts.py projects/spatial-pharma-dl/tests/test_artifact_contract.py</automated>
  </verify>
  <done>Only stable fingerprint-matched bytes can reach a production reader, and every failure remains typed, bounded, and side-effect free.</done>
</task>

<task type="auto" id="4-01-03">
  <name>Publish validated payload/manifest pairs atomically under complete fault injection</name>
  <files>utils/artifacts.py, projects/spatial-pharma-dl/tests/test_artifact_contract.py</files>
  <read_first>
    <file>utils/artifacts.py</file>
    <file>projects/spatial-pharma-dl/tests/test_artifact_contract.py</file>
    <file>.planning/phases/04-durable-artifact-contract/04-VALIDATION.md</file>
  </read_first>
  <action>Implement `publish_artifact()` with narrow injectable operations for test determinism. Create unpredictable payload and sidecar temps in the prepared final parent and retain only exact current-call paths. Let the adapter write/close the temporary payload and return an exact admitted declared-schema candidate from its already trusted in-memory inputs (or accept that candidate as a separate publisher argument); fsync the payload, derive checksum/byte count without decoding it, write compact canonical completed sidecar bytes that already name the logical final payload basename, and fsync the sidecar. Only now validate the explicit temporary pair through the publisher-private logical-basename override in generic admission plus the exact production-reader callback used for reuse; the production decoder must never be called before the temporary sidecar exists and generic admission succeeds, and neither manifest nor fingerprint bytes may change between validation and replacement. Then `os.replace(temp_payload, final_payload)`, fsync the destination directory, replace the completed manifest last, and fsync again. Treat every write/fsync/validation/replace/directory-fsync failure as `publication_failed`, cleanup only remaining current-call temps best-effort, and never delete or roll back a pre-existing/final pair. Parameterize failures before/after temp write, payload fsync, sidecar write/fsync, admitted production-reader validation, payload replace, first directory fsync, manifest replace, and final directory fsync for both new destinations and replacement of a valid old generation. After each failure, call the real admission path and assert the state appropriate to the last completed commit step: before payload replacement the old pair (if any) remains valid; after payload but before manifest replacement the mixed pair is rejected; after manifest replacement the new pair may already be valid even when final directory fsync reports failure and must not be rolled back. Prove operation order, same-directory temp placement, suffix preservation support, temp-name non-reuse, unchanged validated sidecar bytes, and successful fresh/replacement publication.</action>
  <acceptance_criteria>
    <criterion>The exact production reader validates the temporary pair before either final replacement.</criterion>
    <criterion>Successful publication orders payload replace, directory fsync, manifest replace, directory fsync and returns an inspectable completed manifest.</criterion>
    <criterion>Every enumerated failure on new and replacement destinations produces only the state allowed by the last completed commit step: old/absent before payload replacement, rejected mixed state before manifest replacement, or the new valid generation after manifest replacement.</criterion>
    <criterion>Cleanup is restricted to exact current-call temps; old finals and unrelated/orphan files are untouched.</criterion>
    <criterion>All pure Phase 4 contract tests and scoped Ruff pass offline.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_artifact_contract.py -k "atomic or fault or replace or fsync or cleanup" &amp;&amp; python -m pytest -q --strict-markers -m offline projects/spatial-pharma-dl/tests/test_artifact_contract.py &amp;&amp; python -m ruff check utils/artifacts.py projects/spatial-pharma-dl/tests/conftest.py projects/spatial-pharma-dl/tests/test_artifact_contract.py</automated>
  </verify>
  <done>The completed sidecar is a tested commit marker and no publication interruption can promote an incomplete or mixed artifact.</done>
</task>

</tasks>

<verification>
1. Run the complete pure contract module and scoped Ruff command from Task 3.
2. In a clean interpreter, import `utils.artifacts` and assert scientific/model libraries are absent from `sys.modules`.
3. Inspect every fingerprint kind for both relevant invalidation and irrelevant stability assertions, including transitive upstream input slots.
4. Inspect the operation log for temp validation before replacement and payload-first/manifest-last fsync order.
5. Confirm no production scientific adapter, cache-format migration, checkpoint loader change, notebook/CLI change, or Phase 5 malicious-deserialization claim entered this plan.
</verification>

<success_criteria>
- One import-light primitive owns canonical manifests, expected fingerprints, stable byte admission, typed reuse state, and atomic publication.
- Every artifact-kind fingerprint has explicit allowlisted science/lineage inputs and proven presentation-only invariance.
- Malformed, stale, incomplete, mixed, raced, truncated, or semantically rejected artifacts never reach or survive the production-reader callback.
- Every atomic fault point is safe for fresh and replacement publication, with the completed manifest as commit marker.
- T-01 through T-05 are closed without claiming safe deserialization of Phase 5 legacy payloads.
</success_criteria>
